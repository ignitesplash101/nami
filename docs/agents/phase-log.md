# nami — phase log (append-only history)

One entry per shipped phase; the newest entry describes current state best.


- [x] **Phase 0** — GCP setup (project, billing, APIs, service account, key, bucket)
- [x] **Phase 1** — Foundation (pyproject, config, market, cache, sample_portfolios, API/web shell)
- [x] **Phase 2** — Factor model (universe, regression, shocks)
- [x] **Phase 3** — Historical analog matcher (events YAML, analogs.py, COVID-verified)
- [x] **Phase 4** — LLM integration (Gemini + grounding + structured output + Scenario/Results UI)
- [x] **Phase 5** — UI build-out (editable Portfolio, Scenario form, Results dashboard with factor/periphery reasoning tables, Methodology rendering `docs/methodology.md`)
- [x] **Phase 6** — Live-LLM evaluation tests (3 network-gated semantic tests) + `docs/backtest_results.md` snapshot template + `docs/methodology.md`
- [x] **Phase 7** — Deploy (Cloud Run + Cloud Build 2nd-gen trigger; public `run.app` link with app-level passcode admin mode)
- [x] **Phase 8** — Advanced attribution: factor-level Conditional Shapley via `shap.LinearExplainer` + `shap.maskers.Impute` against demeaned historical factor returns; experimental narrative Shapley / theme sensitivity (2^N subset Shapley); Results-tab toggle + reasoning table; methodology section with axioms + framing
- [x] **Phase 9** — Streamlit → FastAPI + React/Vite/Plotly.js runtime; yfinance GCS cache + de-duped factor fetch + parallel I/O; in-process warm cache; narrative Shapley parallelization; explicit-only + grouped Shapley variants; React 4-mode attribution toggle; PROMPT_VERSION v4 → v5
- [x] **Phase 10** — Iterative shock-adjustment (sliders + LLM patch with scope classifier, server-side provenance via `cache_key`, zero-removal carve-out, `model_copy`-enforced invariants); scenario speedups (yfinance hoisted above the Gemini chain, parallel Shapley fits); SSE progress streaming via `/api/scenarios/run-stream` with a React stepper UI
- [x] **Phase 10b** — Methodology UX refactor: slide-in `MethodologyDrawer` (replaces inline panel) with section-accordion + scroll-spy nav; `AttributionGuide` card under the attribution toggle with method-comparison + deep link; clickable factor names in the Results shock table that deep-link the drawer to `factor-universe`; expanded `docs/methodology.md` with end-to-end worked example covering all four attribution variants and an attribution method decision table
- [x] **Phase 10c** — Mobile-friendly workbench: responsive breakpoints (phone ≤640, tablet ≤1079.98, desktop ≥1080), off-canvas left rail via `RailDrawer` + hamburger button on tablet/phone, shared `useOverlay` + `useMediaQuery` hooks, mutually-exclusive drawers, baseline + media-query touch-target bumps (44px iOS/Material), Plotly mobile layout (320h / -90° ticks / automargin), iOS Safari `100dvh` fix, a11y baseline (aria-modal + focus return) on both drawers
- [x] **Phase 11** — Dated/backdated scenarios with strict vintage control (NYSE calendar resolution, end-date analog filter, warm-cache bypass, analog-only narrative); Firestore-backed saved analytics (tagged/named/permalinked scenarios + portfolio snapshots, full inline reproducibility); `PROMPT_VERSION` v5 → v6
- [x] **Phase 12** — Distinctive visual redesign ("Hokusai Deep" indigo/sand/crest token system, Fraunces display type, wave-propagation SSE + ripple reveal + seigaiha brand mark) + accessibility (global `:focus-visible`, `useFocusTrap` on all overlays, real input labels, radiogroup attribution control, `aria-describedby` errors, `prefers-reduced-motion`) + correctness hardening (yfinance network timeout, hallucinated-analog → 422 guard, SSE idle timeout, decomposition-cache validation, adjustment-slider domain fix, `useOverlay` listener-churn fix); no `PROMPT_VERSION` bump
- [x] **Phase 13** — Mark-to-market: share-quantity valuation marked to the as-of RAW close, FX-converted to USD (`app/data/marking.py`); NAV + price-derived weights feed the unchanged linear engine and the dollar view is a client-side `field × NAV` overlay; return-space-only caching (NAV/marks never cached → no stale dollars), mark-before-LLM in quantity mode, fail-closed (`MarkingError` → 503), explicit FX pairs + per-instrument mark dates; admin-only `Shares (MTM)` editor + NAV field + `%/$` results toggle + Marks table; no `PROMPT_VERSION` bump
- [x] **Phase 14** — Notional dollar view as an instant client-side NAV knob (everyone incl. visitors; reserved "MTM" for admin shares); original→stressed position valuation table (Weight/Value/Stressed/Δ$/Δ% + impact/worst/best sort, always-on when NAV exists); theme sensitivity hardened + reframed as fixed-context narrative Shapley (pinned analogs + no re-grounding → deterministic) over a new SSE `/decompose-stream` with X/Y progress + dollarized contributions; no `PROMPT_VERSION` bump
- [x] **Phase 15** — Portfolio-quality increment: realistic cap-weights from a frozen, dated snapshot (`scripts/refresh_sample_weights.py` → `sample_portfolio_weights.json`, hard-fail load); benchmark + active return as a non-cached overlay (one-holding portfolio through the run's own shocks; attaches on hit/miss/adjust; client-resent custom benchmark; `msci_world`→URTH, `us_tech_growth`→QQQ, `defensive_mix`→SPLV, `japan_equity`→EWJ); sector/country exposure breakdown (baked `ticker_meta` + `/api/portfolios/ticker-metadata` + `charts.ts::groupByTag`); `CASH` zero-exposure sleeve (zero-beta row, USD-amount in MTM, all-cash rejected) + adjust-from-canonical weight-drift fix; cost basis deferred; no `PROMPT_VERSION` bump

- [x] **Phase 16** — Fintech-grade operational hardening (single-tenant; no accounts/billing) + minimalist UX increment. **Backend:** per-IP rate limiting (`slowapi`, trusted-IP keying) on the 5 paid endpoints + unlock; transactional daily **cost** breaker + run cap + token metering via a `MeteredGeminiClient` chokepoint; durable transactional passcode brute-force lockout; structured JSON logging + `X-Request-ID` middleware (propagated into SSE worker threads via `copy_context`); `/api/ready` dependency probe (distinct from `/api/health`); audit trail + `/api/export` + `/api/admin/purge` (walks snapshot subcollections, preserves `audit_log`) + public `/api/status` + admin `/api/usage`; opt-in CORS + optional Sentry. **Frontend:** answer-first **Impact summary** readout (`charts.ts::buildReadout`), prompt-led suggestion **chips**, ⌘K **command palette** (accelerator — every command also has a visible control). No `PROMPT_VERSION` bump (all post-pipeline / display-only); disclaimer wording unchanged.
- [x] **Phase 17** — Practical risk-workbench cleanup: visitor/admin share sample chips plus Custom state; visitor custom text is accepted only on sample portfolios; factor labels render as human label plus ticker via `/api/factors`; main attribution exposes **Scenario shocks** and **Group totals** only; `Naive algebra` and `Full conditional diagnostic` move to advanced diagnostics; full conditional is correlation credit/non-causal and never drives the headline; deterministic `risk_diagnostics` added; `PROMPT_VERSION` v7 → v8.
- [x] **Phase 18** — Financial-methodology hardening. **Engine:** standardized (unit-variance) ridge with raw-unit rescale + homogeneous shrinkage; per-ticker NaN masks + 40-week history floor (`InsufficientHistoryError` → 422); per-name fit stats (`r2`/`n_obs`/`idio_vol_weekly`) surfaced as cached `ScenarioResult.regression_quality`; new required `regression_spec` cache-key component (the engine-math invalidation lever). **Shock semantics (v8 → v9):** explicit units/horizon contract in the extraction prompt (cumulative episode total returns, not weekly) + honest rule-4 wording; per-event factor returns + window lengths in the payload and on the result (`analog_event_returns`); horizon-neutral factor descriptions; 2–5 analog cardinality enforced post-hoc; periphery hard band ±0.75 + advisory 0.35. **FX:** non-USD ticker returns USD-converted before betas (`app/data/fx.py`) so the Japan book's active return vs EWJ is currency-consistent. **Diagnostics:** `low_regression_r2`, `position_loss_exceeds_100pct` (never clamp), `periphery_magnitude`, `periphery_dominance`. **Adjustments:** count<3 keep-or-remove carve-out (server-side, mirrored by disabled UI sliders). Docs drift sweep (worked-example references, Monday-anchored bars, blocking-validator framing, cache-key listing, MTM staleness wording, theme-sensitivity framing).
- [x] **Phase 19** — Enterprise-grade UX hardening. **Error contract:** additive `X-Error-Code` header + SSE `code` field with a typed frontend `ApiError` layer (per-cause copy + CTAs, request-id refs with copy, the three 429 causes distinguished); decompose endpoint's mid-compute budget hole fixed (was a 500). **Resilience UX:** toast notifications, root `ErrorBoundary`, `ConfirmDialog` (type-to-confirm) replacing native `confirm()`, run/decompose cancellation with an abort+sequence guard closing the stale-stream race, silent admin-cookie-expiry detection with an unlock banner. **Ops console:** admin drawer over `/api/usage` + `/api/status` + `/api/audit` + `/api/export` + guarded `/api/admin/purge` (purge remounts both admin panels). **Ergonomics/presentation:** wide screens stay wide (1800px bound; ≥1440px the waterfall + exposure cards share a 3fr/2fr row; 75ch prose cap), sticky results toolbar, run→results auto-scroll, first-run skeleton + stale-dim re-runs, onboarding empty state, right-aligned tabular numerals + generalized sortable headers + overflow-gated scroll fades (`useScrollFade`/`TableScroll`), per-table CSV export (formula-injection-guarded), consolidated formatters (`format.ts`), Plotly themed from CSS tokens, spacing/z-index tokens + thin scrollbars. No `PROMPT_VERSION` bump.
- [x] **Phase 20** — Measure & show (senior-quant review, tranche 1). **Analog replay range:** factor-only per-analog replay of each selected event's realized factor returns through the run's betas (`app/factors/shocks.py::analog_replay_pnl` → `ScenarioResult.analog_replay`, cached-with-canonical, preserved by adjustments), rendered as an `AnalogReplayStrip` under the Impact summary with min/median/max, per-event coverage, and a factor-only/not-a-forecast caption — closes the "unshocked factors sit at zero" hole and shows each analog's full realized severity against the banded headline. **Engine-replay validation harness:** LLM-free `app/factors/engine_replay.py` + `scripts/run_engine_replay.py` replaying every (registry event × sample book) pair with vintage betas vs realized buy-and-hold USD returns; regenerated artifacts (`scripts/engine_replay_output.json`, `docs/engine-replay-validation.md`) with MAE/bias/sign-rate/Pearson + per-pair skip reasons; decomposes full-run error into engine vs shock-severity layers (first snapshot: r≈0.98, MAE≈4% over 48 pairs — the engine tracks; the severity gap lives in the LLM/band layer). **Deploy gates:** cloudbuild.yaml runs backend (ruff/black/pytest) + frontend (typecheck/vitest) test gates in parallel before build/deploy. **Docs:** methodology "Analog replay" section + Calibration-evidence cross-links, case-studies stale-wording fix + gap-decomposition addendum, `.claude/worktrees/` gitignored. No `PROMPT_VERSION` bump (additive default field; no prompt/schema-semantics change).

- [x] **Phase 21** — Honest uncertainty (senior-quant review, tranche 2). **Idio dispersion band:** ±1σ = `√(Σ(wᵢσᵢ,idio)²)·√h` over the median selected-analog horizon, surfaced as `ScenarioResult.pnl_uncertainty` + a muted suffix on the Impact-summary P&L (dispersion floor, never a CI; shock-independent; cached-with-canonical; identical on adjustments). **Regression hygiene:** ridge effective dof `p_eff`, dof-honest adjusted R² (persisted), raw-unit beta SEs via the ridge sandwich (in-process, statsmodels-pinned); `low_regression_dof` warning below 3 obs per effective parameter. **Alpha decision (evidence-based):** the review's "raise alpha" hypothesis was REJECTED by a harness sweep (shrink-toward-zero degrades stress tracking monotonically above α≈2); default stays 0.1 with disclosure instead of shrinkage. **Evidence diagnostics:** `band_coverage` (K of N material shocks unbanded) + `scenario_vs_replay` (scenario milder/harsher than every analog replayed). No `PROMPT_VERSION` bump (additive default fields + post-processing only).

- [x] **Phase 22** — Engine breadth (senior-quant review, tranche 3a). **Universe 22 → 26** (`PROMPT_VERSION` v9 → v10): `HYG` high-yield credit (the registry is banking/credit-heavy and had NO credit lever), `GLD` gold (flight-to-quality), `SHY` short-duration Treasuries (front-end rates), `EFA` developed ex-US equities. **Registry 17 → 31 events** (+ tags `technology`/`volatility`/`credit`/`disaster`): dot-com break, 9/11, Enron/WorldCom, Shanghai 2007, Tohoku 2011, CHF unpeg 2015, Volmageddon 2018, 2019 trade-war re-escalation, vaccine rotation 2020, rates tantrum 2021, Evergrande 2021, higher-for-longer 2023, Fed-pivot rally 2023, DeepSeek AI-capex shock 2025 — including recovery and rotation episodes. **Harness regenerated on the new baseline:** 124 pairs / 85 computed, MAE 3.25% (was 3.97%), bias −1.40% (was −2.13%), sign 93%, r 0.976; adding EFA cut the Japan book's bias +2.2% → +0.7%. **Dimson lag evaluated and rejected by evidence** (Japan overstates, not understates — dated decision record in methodology). Sequencing note: breadth shipped BEFORE the stressed-beta experiment so that experiment runs against the final baseline.
- [x] **Phase 23** — Stressed-beta experiment (senior-quant review, tranche 3b) + mobile/zoom QA hardening. **Experiment: both variant families REJECTED by harness evidence** — downside-WLS degrades monotonically in the stress weight (MAE 3.25%→4.18%, r 0.976→0.955 by k=25); informative-target ridge (β=1 SPY/EFA prior) is a wash at α=2 and worse beyond; estimator stays `ridge-std-v2|alpha=0.1`, dated decision record in methodology; clone validated float-exact against production before any variant ran, identical 85-pair set across all 11 runs. **Hardening:** warm cache no longer memoizes degraded `(raw, None)` factor history (a rate-limited boot no longer pins the process to naive-only attribution). **Mobile/zoom QA (Playwright, 7 viewports × app states):** fixed phone-tier horizontal overflow (bare-`1fr`/implicit grid track minimums adopting the 480px table floor), touch-target bumps (chips/guide-toggle/drawer-close/NAV input/copy button), rem-only font sizes (browser text-size zoom), touch-visible ±1σ idio-band framing via `@media (hover: none)`. No `PROMPT_VERSION` bump; no `REGRESSION_ESTIMATOR_ID` bump (no engine-math change shipped).
- [x] **Phase 24** — Free pre-scenario book profile (senior-quant review, tranche 4, P1). `POST /api/portfolios/profile` runs the engine-only path (weekly prices → USD conversion → warm factor cache → cash-aware ridge) and returns portfolio factor exposures (Σᵢwᵢβᵢ,f — the exact multiplier a scenario shock hits), per-name fit quality (weight / adjusted R² / usable weeks / weekly idio vol), and the 1-week ±1σ idio dispersion floor. Zero Gemini, not metered, `llm_limit`-throttled (yfinance fan-out); visitors profile sample books, admins custom weight books. UI: "Profile this book — free, no LLM" CTA + `BookProfileCard` (CSS exposure bars + TableScroll fit table) in the results empty state, cleared on book changes. Methodology "Book profile" section; no `PROMPT_VERSION` bump.
- [x] **Phase 25** — All-events replay screen (senior-quant review, tranche 4, P2). `POST /api/portfolios/events-replay`: every registry event's realized factor returns through the CURRENT book's betas (`analog_replay_pnl`), sorted worst-first, window + factor coverage disclosed. Zero Gemini; shares `_book_betas` + `_run_free_engine_endpoint` with the profile; events × factors matrix process-cached per `events_version` (all-NaN row = transient, never memoized). UI: "Replay every historical event — free, no LLM" CTA + `EventsReplayCard` (TableScroll, loss/gain toning, CSV export) in the results empty state. Methodology "Event replay screen" section; no `PROMPT_VERSION` bump.
- [x] **Phase 26** — Severity ladder + PIT-drift banner (senior-quant review, tranche 4). **Ladder:** `ScenarioResult.severity_ladder` = exact worst/base/best engine P&L over the per-factor `[p10,p90]^K` shock box (vertex by exposure sign; count≥3 band gate shared with the sliders; low-evidence shocks held, removals contribute nothing, periphery constant); `worst ≤ base ≤ best` + `base == total_pnl` pinned; cached with the canonical, recomputed on adjustments (shock-dependent); `SeverityLadderStrip` renders after the analog replay strip with the same NAV dollarization; zero LLM. **PIT banner:** `AccessResponse.sample_weights_as_of` + a backdated-banner line disclosing that sample-book weights are today's frozen snapshot (sample books only). Methodology "Severity ladder" section + "not point-in-time on holdings" bullet; no `PROMPT_VERSION` bump.
- [x] **Phase 27** — Scenario comparison (senior-quant review, tranche 4, P3): pin & compare. "Pin to compare" in the results toolbar holds the current envelope (`pinnedEnvelope` — untouched by every other result setter); any different displayed result (new run, adjusted variant, opened saved scenario, different book) renders `ComparisonPanel` (new file): headline A/B/Δ with per-side ±1σ bands, factor shocks + contributions aligned with deltas under ONE common attribution method (labeled), top name-level movers with absent-side dashes, CSV export, books-differ + as-of-differ disclosures. Pure frontend (identity via `sameScenarioResult` on always-present result fields — cache_key/reproducibility are null on adjust/decompose/saved paths); percent-only; works for visitors. Methodology "Scenario comparison" section; no `PROMPT_VERSION` bump.
- [x] **Phase 28** — UX consolidation: simpler by hierarchy, not by removal. **Evidence & bounds**: the three stacked evidence strips (±1σ idio framing, analog replay range, severity ladder) merged into one `EvidenceBlock` on a shared CSS range gauge (`buildEvidenceGauge`), honesty phrases verbatim in one merged caption, per-analog rows behind a disclosure, old payloads render partial layers. **Tiering**: new `CollapsibleCard` primitive with information-bearing summary headers wraps the drill sections (exposure, factor/name tables, valuation, narrative+analogs, theme sensitivity, admin panels) — desktop opens, compact collapses (once, at mount); the answer layer (readout, evidence, toolbar, waterfall) never collapses. **Empty state**: one sentence + one `KnowYourBook` card (Profile | Event replay tabs) replacing 3 steps + duplicate chips + two CTAs + two cards. **Cuts**: theme sensitivity admin-only; `AttributionGuide` → one methodology deep-link line; ⌘K button hidden ≤640px. **Measured**: phone-390 results page 6,172px → 3,024px (−51%), 63 → 24 interactive controls; desktop unchanged (4,231→4,178px, all sections open). Disclaimer, shock-vs-contrib vocabulary, and every honesty caption unchanged; no `PROMPT_VERSION` bump.
- [x] **Phase 29** — Productionize: fast, installable, frictionless. **Wire weight:** first-load JS 5,282KB uncompressed → 269KB (82KB gz over the wire) — Plotly swapped to the lazily-loaded `plotly.js-finance-dist-min` partial via `PlotLazy.tsx` (waterfall verified in-browser), MethodologyDrawer lazy + latch-mounted, fonts moved to index.html preconnect+link; `SseAwareGZipMiddleware` (SSE paths excluded, cache-miss frame liveness verified) + immutable `/assets/*` / no-cache index Cache-Control split. **Identity:** favicon.svg (seigaiha waves) + .ico + apple-touch-icon + webmanifest + robots.txt in `frontend/public/`, full head meta (description/OG/theme-color/`viewport-fit=cover`), safe-area insets ≤640px. **Serving:** unknown `/api/*` → JSON 404 (was masked HTML 200); catch-all accepts HEAD. **Friction/a11y:** Ctrl/Cmd+Enter runs, passcode Enter-submit form + password autocomplete, dynamic tab title with headline P&L, NAV select-on-focus, one polite live region announcing run stages + completion, `aria-current="step"` stepper, skip link, topbar h1 (visible on phones), footer rendering the `disclaimers.FOOTER` copy. No `PROMPT_VERSION` bump (zero pipeline/schema changes).
- [x] **Phase 30** — Long-haul reliability: no more "network error" on long runs. **Diagnosis (live logs + code trace):** run-streams took 55–98s with the `narrative` stage a 30-90s zero-byte gap; cold events-replay took 120-125s with zero bytes until done; Cloud Run's 300s ceiling severed >5-min decomposes mid-stream — all surfacing as client `kind: "network"` while the server finished with 200. **Server:** shared `_sse_stream_response` factory heartbeats both streams (`queue.get(timeout=10)` → `": keepalive\n\n"`) + `no-cache`/`X-Accel-Buffering: no` headers; Gemini calls bounded at 120s via `HttpOptions(timeout=ms)` with SDK timeout types re-raised as `RuntimeError` into the existing error ladders; startup warming moved to a background daemon thread (port binds immediately; the events matrix pre-warms off-request). **Client:** SSE idle timer re-arms on any received bytes (heartbeat-aware); `requestJson(timeoutMs)` bounds the three long plain POSTs at 240s with kind `timeout` honest copy; mid-stream drops carry "Connection dropped mid-run — retrying can be instant"; profile/replay Retry buttons actually retry. **Infra:** Cloud Run `--timeout` 300→900. Verified: keepalives observed at 10s cadence through a live 28s narrative gap; browser run end-to-end green. No `PROMPT_VERSION` bump.

- [x] **Phase 31 — Retail-first redesign.** **A. Dual theme ✅:** "Hokusai Air" light theme (Prussian ink on washi cream) as a full token-override block; ~36 hard-coded color literals swept into purpose tokens; FOUC-free boot script in index.html; Sun/Moon topbar toggle + ⌘K action (system-follow until explicit choice, persisted); per-theme `chartTheme()` cache (live Plotly re-theme verified in-browser); `--accent-ink` foreground split; brand crest → mask-image. **B. App.tsx decomposition ✅ (behavior-identical):** 2,624 → ~790 lines; components moved to `panels/` + `results/` + `holdings.ts`, state machinery to `state/` (access session, overlay manager, free analytics, run controller); identical DOM/classnames; ResultsPanel hooks hoisted above the empty-state early return; verified by full suite + live browser flow (profile → run → pin → theme flip → drawer). **C. Tabbed IA + demo framing ✅:** top-level areas Scenario / Your book / Library-admin behind a shared accessible `Tabs` primitive (mounted-hidden panels, `?view=` deep links); results drill layer split into Drivers / Positions / Story / Advanced sub-tabs under an always-visible answer band (waterfall in the default tab); `KnowYourBook` promoted to the Your-book area with an empty-state CTA; visitor chip → "Demo mode"; wide-grid pairing moved inside the tab panels; verified in-browser both themes + phone. **D. Fluid responsive ✅:** clamp() type scale + structural spacing (360→1440 interpolation, no breakpoint jumps), fluid rail `clamp(300px, 24vw, 380px)`, tab-panel pairing lowered to ≥1280px with a position-scoped empty-exposure fallback, 720–1079.98 tablet tier re-pairs `.two-column`; verified by a 9-width sweep (360→1920, zero horizontal overflow, smooth h1 interpolation) + live pairing checks at 1320. **E. Retail copy pass ✅:** glossary + InfoTip layer (plain-first, quant detail one tap away, honesty caveats preserved verbatim in the detail layer); answer-layer relabels (h1, waterfall heading + subtitle, theme card, single-name-noise chip, historical events/sources); invariant strings untouched. **F. Agent-docs split ✅:** the monolithic CLAUDE.md/AGENTS.md gotcha list (220 bullets) mechanically redistributed VERBATIM into eight owning docs under `docs/agents/` (engine, llm-pipeline, data-market, api-backend, frontend, design-system, deploy-ops, phase-log) with exactly-once accounting; both roots became byte-identical ~115-line routers (mission, stack, commands, gates, cardinal rules, routing table); `tests/test_agent_docs.py` fails CI on router drift or a missing area doc. No `PROMPT_VERSION` bump (the whole Phase 31 arc is display/docs-only). **G. Adjust flow redesign ✅** (user feedback: the bottom tweaking card was poorly designed and didn't stretch): Adjust became a results sub-tab under the answer band; prompt-first layout; keep-or-remove factors (count<3 — the COMMON case, e.g. 26/26 on a 2-analog run) collapsed from ~26 dead slider rows into a removable-chip grid; tunable rows impact-sorted with percent-unit inputs; exact client-side P&L preview (naive-linear, verified equal to a live server recalc); wide screens flow multi-column.

Source of truth for phase scope: the Phase status list above. (README.md deliberately carries the feature-level view only — it has no phases section; don't reintroduce a dangling `README.md#implementation-phases` link here.)

# CLAUDE.md — nami AI Agent Dev Notes

Quick reference for AI coding agents (Claude Code sessions, etc.) working on nami. **Read this first** before making changes.

Full architecture and roadmap live in [README.md](README.md). This file is the agent quick-start.

---

## Mission

nami (波) is an LLM-driven hypothetical stress explorer for equity portfolios. User describes a market stress in natural language → Gemini grounds it against current news + historical analogs → derives factor shocks → the engine computes modeled portfolio P&L with cited reasoning.

**Educational/research tool only** — not investment advice, not a forecast. The disclaimer is enforced in `app/utils/disclaimers.py` and rendered on every page.

---

## Stack at a glance

- **Python 3.12** (pinned in `.python-version` and `pyproject.toml`)
- **uv** for env/dep management
- **FastAPI** backend at `app/api/main.py`
- **React + TypeScript + Vite + Plotly.js** frontend under `frontend/`
- **GCP** — Vertex AI (`gemini-3.5-flash`), Cloud Storage (cache), Cloud Run (deploy)
- **Region split** (this matters):
  - Cloud Run / GCS bucket / Artifact Registry → `asia-northeast1`
  - Vertex AI → `global` (gemini-3.5-flash is NOT on `asia-northeast1` or `us-central1`)

---

## Where things live

```
app/
├── config.py                # env-var loader; validates 3 REQUIRED keys
├── api/                     # FastAPI endpoints, auth/session cookies, API schemas
├── data/
│   ├── market.py            # yfinance wrapper: fetch_weekly_prices, compute_weekly_returns
│   ├── cache.py             # CloudStorageCache: parquet I/O with TTL via blob.updated
│   └── sample_portfolios.py # 4 curated portfolios (Portfolio dataclass)
├── factors/                 # Phase 2 — factor model (universe, regression, shocks)
│                            # Phase 3 — historical analog matcher (analogs.py)
├── llm/                     # Phase 4 — Vertex AI / Gemini integration
│                            #   schemas.py · prompts.py · grounding.py · validation.py
│                            #   gemini_client.py · scenario.py (orchestrator)
└── utils/
    └── disclaimers.py       # disclaimer strings + footer

frontend/                    # React/Vite workbench; Plotly.js client-side charts
tests/                       # pytest unit tests, in sync with the implementation phase
```

---

## Commands you'll actually use

```powershell
# from <repo root>

uv sync                            # install/update deps from pyproject.toml
uv run uvicorn app.api.main:api --reload --host 0.0.0.0 --port 8080
cd frontend; npm install; npm run dev
uv run pytest tests/ -v            # run unit tests
uv run ruff check .                # lint
uv run black --check .             # format check
uv run black .                     # format fix
cd frontend; npm run typecheck; npm test; npm run build
uv run python scripts/run_engine_replay.py   # regenerate engine-replay validation artifacts (network)
```

To exercise the GCS cache or Vertex AI, the local `.env` must have all 4 REQUIRED keys populated. Template: `.env.example`.

---

## Coding conventions (specific to this repo)

- **Type hints required** on every public function (params + return).
- **`from __future__ import annotations`** at the top of every new module — keeps type hints as strings so `dict[str, float]` / `X | None` work without runtime cost.
- **`@dataclass(frozen=True)`** for value objects (`Config`, `Portfolio`, `Factor`).
- **Validate at boundaries.** Raise `ValueError` / `RuntimeError` with specific messages when inputs violate invariants. See `Portfolio.__post_init__` (weight sum) for the pattern.
- **No `print()` in library code** — `print` is for `__main__` smoke checks or CLI scripts only. FastAPI code should use structured responses/exceptions; frontend code should surface user-facing errors in state.
- **No commented-out code, no `# TODO:`** — if you're not implementing it now, raise `NotImplementedError` and open a separate scope.
- **Comments only when the WHY is non-obvious.** Names + types do the explaining. Comments document irreducible context (a workaround, a constraint).

---

## Quality gates before "done"

1. `uv run pytest tests/ -v` — all green
2. `uv run ruff check .` — clean
3. `uv run black --check .` — clean
4. `cd frontend; npm run typecheck; npm test; npm run build` — frontend green
5. For UI changes: run FastAPI + Vite locally and verify in the browser. Type-checking passing ≠ feature working.

---

If a long-lived local `.venv` has a stale/broken Black entrypoint, use
`uvx --python 3.12 --from black black --check .` to match fresh CI behavior.

## Known gotchas

- **yfinance silently drops tickers it can't fetch.** Any function that maps tickers → returns must validate set membership and raise loudly. Documented at `app/data/market.py` (the dropna step).
- **PowerShell `python -c "..."` collides with embedded `f"..."`.** The outer `"` eats the inner `"`. Workarounds: single-quoted f-strings (`f'...'`), here-strings, or a real `.py` file.
- **Parquet round-trip normalizes datetime precision** (`datetime64[s]` → `datetime64[ms]`). Values are identical; only the index dtype changes. Use `assert_frame_equal(..., check_index_type=False)` in tests.
- **`fetch_weekly_prices(..., lookback_weeks=N)` anchors on `end` if provided, else `utcnow()`.** Both `fetch_weekly_prices` and `fetch_daily_prices` share an internal `_fetch_prices` helper — if you touch one, verify the other.
- **yfinance's `end` is EXCLUSIVE.** `start=2020-02-19, end=2020-03-23` returns bars through 2020-03-20, NOT 2020-03-23. `fetch_event_returns` in `app/factors/analogs.py` adds 1 day before calling yfinance to get inclusive-end semantics; callers wanting "include the end date's close" must do the same +1-day transform.
- **yfinance `interval="1wk"` returns Monday-anchored bars, not week-ending Fridays.** `fetch_weekly_prices(start="2020-02-19", end="2020-03-23")` returns a first row dated **2020-02-17** (Monday). This breaks "first Friday ≥ start" intuition — Phase 3 uses `fetch_daily_prices` instead to get exact-day alignment for event windows.
- **Scenario cache key MUST include all semantic inputs.** `scenario_cache_key()` hashes scenario_text + portfolio_key + holdings dict + market_date + model_id + prompt_version + factor_universe_version + events_version + **regression_spec** (required — the engine-math lever, see below), **plus position_quantities and pinned_event_ids when present**. Dropping any of these would silently return wrong P&L (e.g. same tickers + different weights produces different P&L but would have hit the same cache entry).
- **`regression_spec` is the engine-math cache lever (Phase 18); PROMPT_VERSION is the prompt/schema lever.** `app/factors/regression.py::regression_spec()` renders `"<REGRESSION_ESTIMATOR_ID>|lookback=…|alpha=…|min_obs=…"` and is a REQUIRED kwarg of `scenario_cache_key` — changing the estimator math (bump `REGRESSION_ESTIMATOR_ID`), `RIDGE_ALPHA`, or `BETA_LOOKBACK_WEEKS` self-invalidates every cached result. Do NOT bump PROMPT_VERSION for engine-math changes.
- **`portfolio_pnl()` returns JSON-safe dicts** (not pd.Series) since Phase 4 — Series would break the cache serialization round-trip. Shape includes `total_pnl`, the explicit attribution maps (`by_factor_naive`, conditional variants), and `by_ticker_factor` / `by_ticker_periphery` / `by_ticker_total`; all values are plain floats or dicts of floats.
- **Gemini grounding + `response_schema` in one call is unreliable.** `gemini-3.5-flash` can return valid JSON while skipping `google_search`, leaving `grounding_metadata` empty. `propose_shocks_with_retry` deliberately splits Call 2 into `_grounded_narrative` (text + Google Search, no schema) and `_extract_structured_shocks` (schema, no tools). Do NOT merge them back together or add a "retry without grounding" fallback.
- **`run_scenario` accepts injected `gemini` and `cache`** — tests use mocks (`tests/conftest.py::InMemoryCache` and `_MockGeminiClient`) instead of `storage.Client()` and `genai.Client()`. Production code calls with defaults (which read `Config` and construct real clients).
- **PROMPT_VERSION** in `app/llm/prompts.py` MUST be bumped for ANY change that affects `ScenarioResult`'s shape OR prompt semantics. Schema changes invalidate the cache exactly the same way prompt changes do. v3 means `ScenarioResult` carries `portfolio_name` + `portfolio_holdings`.
- **`ScenarioResult` is self-contained as of v3.** Always render holdings from `result.portfolio_holdings`, never from `get_portfolio(result.portfolio_key)` — the latter raises on `"custom"`. The new fields have defaults so cached v2 entries don't crash, but they'll show weight=0 for tickers; the PROMPT_VERSION bump invalidates them lazily.
- **Results-tab labels must keep `shock applied` vs `contrib to P&L` distinct.** "Shock applied" is the magnitude the LLM proposed for a factor; "contrib to P&L" is `(Σᵢ wᵢ · βᵢ,f) · shock[f]` — the weighted contribution to the total. Never collapse these into a single number.
- **`run_scenario` accepts EITHER `portfolio` (positional, `str | Portfolio`) OR `portfolio_key=` (kwarg, back-compat).** Passing both or neither raises `ValueError`. The UI passes a `Portfolio` object so custom holdings round-trip; existing tests still pass `portfolio_key="us_tech_growth"`.
- **CSV/custom portfolio rules** live in `app/api/portfolio_validation.py`: uppercase tickers (`.T` suffixes preserved); reject blanks / duplicates / negatives / non-finite; totals near 1.0 accepted as decimals OR near 100 as percentages (auto-normalized to decimals). Anything else is rejected.
- **`tests/test_live_evals.py` is network-gated on `RUN_NETWORK_TESTS=1`** and **stochastic over news drift** even with `temperature=0` — use semantic assertions only (tag membership, ordering, presence-of-citation), never magnitude bounds. `docs/backtest_results.md` is a DATED snapshot, not a permanent benchmark. Costs ~$0.001 per test.
- **Cloud Run GCP client auth uses ADC**, not JSON keys. `nami-sa` is attached via `--service-account=` in `cloudbuild.yaml`; the Vertex AI + GCS Python clients pick it up automatically. Do NOT set `GOOGLE_APPLICATION_CREDENTIALS` on Cloud Run — `app/config.py` tolerates its absence.
- **Cloud Run is publicly invokable, but the app has passcode admin mode.** `cloudbuild.yaml` uses `--allow-unauthenticated` so the React app is viewable without Google login. The `PASSCODE` value is injected from Secret Manager (`nami-passcode:latest`). Visitor mode allows sample scenarios or custom scenario text on sample portfolios only; admin mode unlocks custom/uploaded portfolios, benchmark overrides, backdating, MTM, saved analytics, adjustments, and theme sensitivity.
- **FastAPI binds to `$PORT`** on Cloud Run via the Dockerfile CMD (`uvicorn app.api.main:api --host 0.0.0.0 --port ${PORT:-8080}`). Don't drop `--host 0.0.0.0` — Cloud Run's TCP health checks fail on default localhost binding.
- **Admin access is an HTTP-only signed cookie**, not frontend-only state. `app/api/security.py` signs the admin cookie with the configured passcode; changing the passcode invalidates existing admin cookies.
- **`--session-affinity` is still enabled** in `cloudbuild.yaml`. It is less load-bearing after moving off Streamlit session state, but harmless and useful for long-running requests.
- **Cloud Build 2nd-gen trigger** `nami-main-push` lives in `asia-northeast1`, uses the repository resource `projects/<PROJECT_ID>/locations/asia-northeast1/connections/nami-github-connection/repositories/ignitesplash101-nami`. Edit via Cloud Console → Triggers, or `gcloud builds triggers update`.
- **Cloud Build SA for this project is the compute-engine default** (`<PROJECT_NUMBER>-compute@developer.gserviceaccount.com`), NOT the legacy `<PROJECT_NUMBER>@cloudbuild.gserviceaccount.com`. `gcloud builds get-default-service-account` returns empty in some gcloud versions; derive reliably with `gcloud projects describe $PROJECT_ID --format="value(projectNumber)"` then construct `<NUMBER>-compute@developer.gserviceaccount.com`.
- **Dockerfile is multi-stage.** Node builds `frontend/dist`; Python runs FastAPI. The Python stage still uses `uv sync --frozen --no-dev --no-install-project` + `PYTHONPATH=/app`. Don't drop `--no-install-project` — without it, `uv sync` reads `pyproject.toml`'s `readme = README.md` field and fails because README isn't copied yet in the deps layer.
- **Gemini 3.5 Flash region:** `global` / `us` / `eu` only. NOT `us-central1`, NOT `asia-northeast1`. `VERTEX_AI_LOCATION` is independent of the Cloud Run / bucket region.
- **`PortfolioPnL.by_factor_naive` is the persisted naive attribution.** There is NO `by_factor` alias. Pydantic v2's `computed_field` is included in `model_dump()` but rejected by `model_validate()` under `extra="forbid"`, which would poison the JSON cache round-trip. Readers must pick a specific variant: `by_factor_naive` / `by_factor_conditional_shapley` / `by_factor_conditional_shapley_explicit` / `by_factor_conditional_shapley_grouped`.
- **Four attribution maps live in `app/factors/attribution.py`, but only two are main UI choices.** `conditional_shapley_attribution_explicit` backs **Scenario shocks** (production risk view; explicitly shocked factors only), and `conditional_shapley_attribution_grouped` backs **Group totals** (risk-committee view). The grouped backend map remains factor-level after within-group redistribution; the UI waterfall sums it into true Market / Sector / Style / Macro bars and keeps factor detail in the table. `naive_attribution` and `conditional_shapley_attribution` stay in advanced diagnostics as **Naive algebra** and **Full conditional diagnostic**. The full diagnostic can allocate correlation credit to unshocked factors and must never drive the headline/top-driver readout.
- **The grouped Shapley implementation is "full Shapley + within-group sum-and-redistribute", NOT synthetic-feature aggregation.** Aggregating member coefs+shocks before running shap introduces within-group cross-product terms `c_f · s_{f'}` that don't exist in the linear model. Running full Shapley first then summing φ_f within each group preserves efficiency exactly.
- **`conditional_shapley_attribution_explicit` short-circuits the 1-feature case.** shap.LinearExplainer's covariance path crashes on a 1-D background; for a single explicit shock the trivial Shapley value `coef · shock` is returned directly.
- **Conditional Shapley ≠ causal attribution.** It is *data-dependent credit allocation* under the historical conditional distribution of factor returns. The **full** variant can attribute nonzero credit to factors with zero LLM shock (via correlation). In the UI those rows say `Correlation credit; no explicit shock`. The explicit/scenario view keeps unshocked factors exactly zero.
- **Factor labels are user-facing metadata, not model keys.** `app/factors/universe.py` exposes `key`, `ticker`, `group`, `short_label`, `display_name`, and `description`; `/api/factors` serves them read-only. Render as `Human label (TICKER)` wherever space allows, but do not change internal factor keys or math.
- **`ScenarioResult.risk_diagnostics` is warning-only deterministic post-processing.** It defaults to `[]` for old cached/saved payloads. Diagnostics may flag correlation conflicts, analog-envelope direction conflicts, and full-conditional cross-credit, but they must not rewrite shocks or clamp signs.
- **Use `shap.maskers.Impute(background)`** with the non-deprecated `LinearExplainer((coefs, intercept), masker)` form (see `app/factors/attribution.py`). The `feature_perturbation=` kwarg is deprecated in current shap; new code must avoid it.
- **Factor-return background must NOT `fillna(0)`.** `fetch_factor_returns_history` does `dropna(how="any")` then demeans, requiring ≥52 complete rows. Zero-filling a missing ETF manufactures false correlation that contaminates the Conditional Shapley values. The strict dropna restricts the background to the post-XLC-launch window (mid-2018+), which is correct.
- **Theme sensitivity is experimental fixed-context narrative Shapley**, not causal risk attribution. Each subset PINS the source scenario's analog set and uses the analog-only narrative path (no re-selection, no re-grounding) — only the shock proposal varies per fragment, so subset payoffs are deterministic. Frame this honestly in both UI (section label says "Experimental") and methodology doc.
- **Narrative Shapley lives in `app/llm/narrative_shapley.py::compute_narrative_shapley`, NOT inside `run_scenario`.** The UI calls `run_scenario` first, then `compute_narrative_shapley` if opted-in, which calls `decompose_scenario` + reruns the pipeline 2^N − 1 times and attaches the result via `model_copy(update={"narrative_shapley": ...})`. Putting it inside `run_scenario` would invite recursion and control-flow confusion.
- **`PROMPT_VERSION` covers cache invalidation for prompt OR schema changes.** v3 → v4 because `PortfolioPnL.by_factor` was renamed to `by_factor_naive` + new field added + `ScenarioResult.narrative_shapley` introduced. v4 → v5 added `by_factor_conditional_shapley_explicit` and `by_factor_conditional_shapley_grouped`. The anticipated PROMPT/engine split happened in Phase 18: engine math is now keyed by the separate `regression_spec` cache-key component, so PROMPT_VERSION stays a pure prompt/schema-semantics marker.
- **N must be in [2, 4] for narrative Shapley.** Both `decompose_scenario` and `compute_narrative_shapley` raise `RuntimeError` if the decomposer returns out-of-range. N=5 would be 31 subset runs ≈ 8 min sync wall-clock and is cut off intentionally.
- **Narrative Shapley runs subsets in parallel.** `compute_narrative_shapley` submits all 2^N−1 non-empty subsets to a `ThreadPoolExecutor` (`config.narrative_shapley_max_workers`, default 4) and drains via `concurrent.futures.as_completed` so the progress callback fires from the main thread. `subset_pnls` is keyed by mask, not by completion order — the Shapley math is deterministic regardless of how the pool drained. If Gemini rate-limits show up in practice, drop `NARRATIVE_SHAPLEY_MAX_WORKERS=2`.
- **yfinance is cached at the `_fetch_prices` layer.** `app/data/market.py` consults a process-wide `CloudStorageCache` (prefix `market_data`, parquet, 24h TTL) keyed on `(sorted_tickers, interval, start, end)` before calling `yf.download`. Cache failures (no GCS creds, parquet schema mismatch) silently fall back to yfinance — both read and write paths are wrapped in `contextlib.suppress(Exception)`. The singleton is `app/data/market_cache.py::get_market_cache()` (lru_cache(maxsize=1)); tests pass `cache=None` explicitly to bypass.
- **`run_scenario` parallelizes portfolio-prices vs factor-history fetches.** Inside the cache-miss path, `fetch_weekly_prices(portfolio.tickers)` and `get_factor_returns_with_history(...)` are submitted to a 2-worker ThreadPoolExecutor and joined before `estimate_betas_for_portfolio` runs. The combined factor fetch returns `(raw_returns, demeaned_history_or_None)` — passes BOTH into the beta estimator AND the SHAP background, eliminating a duplicate yfinance round-trip that existed pre-Phase-9.
- **Per-event analog fetching lives in `fetch_event_returns_matrix`** (Phase 18): events × factors matrix, input order preserved, up to 8 concurrent `fetch_event_returns` workers via `executor.map`. `compute_envelope` is now a thin wrapper (`compute_envelope_from_matrix(fetch_event_returns_matrix(...))`); `run_scenario` calls the two pieces directly so the per-event rows feed the extraction prompt + `ScenarioResult.analog_event_returns` without a duplicate fetch.
- **In-process warm cache lives in `app/factors/warm_cache.py`, NOT in `app/ui/`.** Originally planned as `@st.cache_resource`; corrected to `functools.lru_cache(maxsize=4)` so it survives the FastAPI rewrite. `app/factors/analogs.py::load_events`/`event_summaries`/`events_version` and `app/factors/universe.py::factor_universe_version` are also lru_cached. Call `warm_cache.warm()` from a FastAPI `lifespan` hook to pre-populate.
- **Mocks for `run_scenario` MUST stub FOUR market-layer functions** in the scenario module's namespace (per the parallelized hot path): `app.llm.scenario.fetch_weekly_prices`, `app.llm.scenario.get_factor_returns_with_history`, `app.llm.scenario.estimate_betas_for_portfolio`, and `app.llm.scenario.convert_weekly_returns_to_usd` (identity stand-in — keeps non-USD books network-free). See `tests/test_scenario.py::_patch_market_layer`. The beta-estimator mock must accept `**kwargs` AND return the `(betas, stats)` tuple (`stats` is `dict[str, TickerRegressionStats]`) because the orchestrator unpacks it.
- **yfinance is hoisted above the Gemini chain in `run_scenario`** (Phase 10). The ThreadPoolExecutor that fetches portfolio prices ∥ factor history is opened immediately after the cache check and stays open across `select_analogs` → `compute_envelope` → `propose_shocks_with_retry`. yfinance becomes effectively free (~2-6s) by overlapping with the ~8-16s Gemini chain. Don't move it back below the Gemini calls "for clarity" — it's a deliberate parallelism win.
- **`run_scenario` accepts an optional `progress: Callable[[str, str], None]` callback** (Phase 10). It fires `progress(stage, status)` at stage boundaries: `cache_check`, `cache_hit`, `market`, `analogs`, `envelope`, `narrative`, `betas`, `attribution`. The SSE endpoint at `/api/scenarios/run-stream` wraps this — the worker thread runs `run_scenario(progress=...)` and a `queue.Queue` carries events to the FastAPI generator. `run_scenario` itself NEVER emits `"done"`; the SSE wrapper emits `{"stage":"done","result":...}` after the function returns.
- **The 3 Shapley fits in `portfolio_pnl` run in a 3-worker `ThreadPoolExecutor`** (Phase 10). `shap.LinearExplainer` drops into SciPy/NumPy linalg which releases the GIL, so threading buys real parallelism (~2-4s saved). Each variant still degrades independently to `None` on exception — the parallelism wraps the same `try/except` pattern, futures-style.
- **Iterative shock adjustment is structured editing, NOT free-form re-proposal.** `adjust_scenario_shocks` in `app/llm/scenario.py` takes a `cache_key` (NOT a client-supplied `prior_result`) and either `overrides: dict[str, float]` (manual sliders) or `adjustment_text: str` (LLM prompt). The server re-fetches the trusted canonical from cache. Narrative, citations, analogs_selected, periphery_shocks, and factor_envelope are guaranteed byte-for-byte unchanged by `model_copy`, not by prompt wording. Only `factor_shocks`, `portfolio_pnl`, and `adjustment_history` change. Derived results are NOT cached.
- **The LLM patch is a separate schema (`ShockEditPatch`), NOT `ShockProposalOutput`.** Reusing the proposal schema would let the model rewrite narrative/periphery, which violates the editing invariants. `ShockEditPatch` carries `scope: Literal["local", "rerun_required"]` + `edits: list[FactorEdit]` + `rejection_reason: str | None`. `scope="rerun_required"` surfaces via HTTP 422 with `rejection_reason` as the detail; the UI offers a "Rerun full scenario" CTA pre-filling the original text + adjustment. Semantic changes (new mechanism, new region, new factor) belong in a rerun, not an edit.
- **Zero-removal carve-out in `app/llm/adjust_validation.py::validate_factor_overrides`:** `new_shock == 0.0` is ALWAYS accepted regardless of envelope (explicit removal sentinel). Otherwise must be in `[p10, p90]` **when the factor's analog `count >= 3`**; for low-evidence factors (`count < 3`, or missing envelope/p10/p90 — incl. the count=0 rows stored as 0/0/0) the only valid values are 0.0 or the canonical shock itself (**keep-or-remove**, `math.isclose` echo check). Note `validate_shock_proposal` is NOT advisory either — its band check (count≥3) blocks after a one-retry repair loop; adjustment just rejects immediately and adds the keep-or-remove rule. The validator also requires `set(overrides.keys()) == set(canonical_factor_names)`; missing or extra keys raise. UI mirrors this by rendering one slider per canonical factor and disabling re-tuning on `count < 3` rows ("n=K analog observation(s) — keep or remove"); the rule binds LLM patches and direct API calls server-side regardless.
- **`ScenarioRunResponse.cache_key` is the server's provenance handle.** The server computes it from `compute_scenario_cache_key(scenario_text, portfolio, market_date=...)` and returns it on both `/api/scenarios/run` and `/api/scenarios/run-stream`. The client echoes it back on `/api/scenarios/adjust-shocks`; the server re-fetches the canonical result and rejects with HTTP 410 if the cache TTL has expired. Don't trust client-supplied `prior_result` payloads — they're tamperable.
- **`ScenarioResult.adjustment_history: list[ShockAdjustment]` is a derived-only field with `default_factory=list`.** Canonical results round-tripped through the GCS cache always carry `[]`. Adjusted results carry a growing list, but adjusted results are never cached. Because the field has a default, the schema change did NOT require a `PROMPT_VERSION` bump — old v5 cached entries deserialize with `adjustment_history=[]`.
- **SSE client uses `fetch` + `ReadableStream`, not `EventSource`.** `frontend/src/api.ts::runScenarioStream` reads chunks and splits on `\n\n` frames manually. Reason: `EventSource` is GET-only and doesn't carry POST bodies; the chunked-reader approach also forwards cookies cleanly on same-origin requests. The terminal event is `{"stage":"done","result":...}`; the stream also handles `{"stage":"error","message":...}` by rejecting the promise.
- **Methodology UI is a slide-in drawer, not an inline panel.** `frontend/src/MethodologyDrawer.tsx` renders a right-side drawer over a backdrop with body-scroll-lock + Esc-to-close (`useMethodologyDrawer.ts`). It auto-parses `docs/methodology.md` by splitting on `\n---\n`, slug-ifies section titles, renders an accordion (one expanded section at a time by default, multiple allowed). Deep links from anywhere in the app open the drawer at a specific section: `methodologyDrawer.open("factor-attribution")` matches the first section whose `slug.includes("factor-attribution")`. **The drawer parses markdown sections at render time, so adding a new section to `docs/methodology.md` is enough — no code change needed for the drawer to pick it up.** New deep-link targets DO need a matching `methodologyDrawer.open("<slug>")` call site.
- **Factor names in the Results table are deep-link buttons** (`frontend/src/App.tsx`, `.factor-link` styles). Clicking a factor opens the methodology drawer at `factor-universe`. The `AttributionGuide` component under the attribution toggle deep-links to `factor-attribution`. Both pass through `ResultsPanel`'s new `onOpenMethodology: (section?: string) => void` prop.
- **`refreshAccess` in `App.tsx` is now load-bearing** (Phase 19): it backs the access watcher, the 403→re-check hook, and the session-expired banner via `applyAccess` (see the Phase 19 gotchas below). Don't reintroduce the inline `MethodologyPanel` — the drawer is the canonical surface.
- **`docs/methodology.md` has a "Worked example" heading** inside the Factor-attribution section that the drawer's `parseMethodologySections` flags (via the `hasExample` regex `/worked example/i`) and the CSS highlights with a subtle border accent. It is now a short **qualitative** 2-factor table (the old numeric 3×2 walk-through was deliberately removed — printed magnitudes were a drift magnet; the invariants are pinned synthetically in `tests/test_methodology_worked_example.py`). Keep the heading text when editing, keep the `factor-attribution` section self-contained for the AttributionGuide deep-link, and do NOT reintroduce a numeric example without pinning the shap version.
- **Responsive breakpoints (Phase 10c):** `@media (max-width: 1079.98px)` is the tablet/phone-landscape tier; `@media (max-width: 640px)` is the phone tier; baseline (≥1080px) is desktop. The desktop boundary is 1080px (NOT 1024px) because `.app-shell` requires 360px rail + 720px workbench = 1080px to render without horizontal overflow. iPad Pro portrait at 1024×1366 renders in tablet mode by design.
- **`body { min-width: 1100px }` is gone (Phase 10c).** Do not reintroduce — it forces horizontal scroll on every viewport below 1100px. The responsive grid below handles layout.
- **The rail (`<aside className="rail">` with AccessPanel + PortfolioPanel) is conditionally rendered.** `frontend/src/App.tsx` uses `useMediaQuery("(max-width: 1079.98px)")` to choose between inline `<aside>` (desktop) and `<RailDrawer>` (tablet/phone, opened via hamburger). The shared JSX is extracted into a `const railContent` and passed as `children` to RailDrawer. Mounting only one prevents duplicated component instances with diverging local state. The two drawers (rail + methodology) are mutually exclusive — opening one closes the other via `openMethodology` / `openRailDrawer` helpers in App.tsx.
- **`useOverlay` is the shared overlay primitive** ([frontend/src/useOverlay.ts](frontend/src/useOverlay.ts)). It handles `isOpen` state, body-scroll-lock, and Esc-to-close. Critically, the `onClose` callback fires BEFORE `isOpen` flips to false — so consumer-specific cleanup (like clearing `initialSection` on the methodology drawer) runs deterministically for both the explicit `close()` call AND the Esc-key path. `useMethodologyDrawer` is now a thin wrapper around `useOverlay({ onClose: () => setInitialSection(null) })`.
- **`useMediaQuery` is SSR-safe** ([frontend/src/useMediaQuery.ts](frontend/src/useMediaQuery.ts)). Returns `false` during SSR; subscribes to `change` events client-side. Used for the rail-render switch in App.tsx and for the Plotly mobile-layout switch in `ResultsPanel`.
- **Plotly mobile tweaks** ([App.tsx](frontend/src/App.tsx) inside `ResultsPanel`): when `isPhone`, height drops to 320, bottom margin grows to 110, `xaxis.tickangle: -90` with `tickfont.size: 9`. Also `xaxis.automargin: true` as belt-and-braces for clipping. Don't truncate bars — vertical labels handle density.
- **Mobile-only CSS rules live at the bottom of `frontend/src/styles.css`** in two `@media` blocks. Above the blocks is the desktop baseline; the blocks layer overrides. `body { min-width }` was REMOVED, not gated. The rail's `height: 100vh` is paired with a follow-up `height: 100dvh` declaration so iOS Safari's collapsing URL bar doesn't leave a gap.
- **Tables: the mobile `.table-scroll > table { min-width: 480px }` rule is scoped — NOT a blanket `table` rule.** Three table surfaces exist: (1) tables inside `.table-scroll` (factor shocks, name-level, analogs, theme sensitivity) — the mobile rule applies; (2) markdown-rendered tables inside `.section-content` (methodology drawer) — they get `overflow-x: auto` on the section instead; (3) `.adjustment-history` list — narrow enough to leave alone. If you add a new wide table, wrap it in `<div className="table-scroll">` so it gets the responsive treatment.
- **The theme-sensitivity `<table>` in App.tsx is wrapped in `.table-scroll`** so it gets the same horizontal-scroll treatment as the other Results tables. Don't remove the wrapper when refactoring; tests don't catch this regression.
- **Hamburger button class `.rail-toggle-btn` extends `.methodology-btn`** styling for visual consistency. It's `display: none` baseline and switches to `display: inline-flex; order: -1` at `@media (max-width: 1079.98px)` (the `order: -1` floats it to the left of the topbar's other children).
- **Touch-target baseline is now 40px** on `.primary-button` / `.ghost-button` / `.segmented button` (was 38px), bumping to 44px at ≤1079.98px. Inputs got `padding: 12px` (was `10px 11px`) baseline + `min-height: 44px` at ≤1079.98px. Slider thumbs are sized to 22×22 globally via `::-webkit-slider-thumb` / `::-moz-range-thumb` — applies to both desktop and mobile for parity.
- **Drawer a11y baseline**: both `RailDrawer` and `MethodologyDrawer` set `role="dialog"`, `aria-modal="true"`, and use `useRef` to (a) focus the close button on open, (b) capture `document.activeElement` before open + restore focus to it on close. Full focus-trap (Tab cycling within the dialog) is NOT implemented — that's a follow-up.
- **iOS Safari `100dvh`**: `.app-shell` and `.rail` use `min-height: 100vh` followed by `min-height: 100dvh` (declaration-order fallback) so older browsers degrade to `100vh`. The `.drawer-panel` and `.rail-drawer-panel` use `top: 0; bottom: 0` fixed-inset positioning, so no 100vh involved there. `.rail-drawer-panel` and `.drawer-body` add `overscroll-behavior: contain` to prevent rubber-band scroll propagating to the body.
- **Scenario-shocks attribution sums to factor-driven P&L** under nami's demeaned-background contract — `fetch_factor_returns_history` demeans before passing to `shap.maskers.Impute`. Factors with shock=0 contribute zero to factor-driven P&L by definition, so the shocked-factor sub-game's grand-coalition value equals the factor-driven P&L. The distinguishing property is that **unshocked factors stay at exactly zero** (no correlation-driven credit). The regression guard is in [tests/test_methodology_worked_example.py](tests/test_methodology_worked_example.py).
- **Attribution invariants are pinned synthetically, not against doc numbers.** `tests/test_methodology_worked_example.py` asserts the math invariants (efficiency, sum-equality across variants, zero-on-unshocked for explicit-only) on synthetic data; `docs/methodology.md` carries no printed magnitudes to test (the worked example is qualitative). Specific Conditional Shapley magnitudes drift across `shap` library versions — do not reintroduce magnitude pinning without also pinning the shap version.
- **Methodology citation links use direct external URLs** (DOI / arXiv / publisher pages), NOT anchor links to the bottom References section. react-markdown does not auto-generate heading IDs in the methodology drawer, so `#references`-style fragments wouldn't navigate. If you add a new citation, link to the canonical paper page externally.
- **The `## References` section at the bottom of `docs/methodology.md` is preceded by `---` on its own line.** This is load-bearing: [MethodologyDrawer.tsx::parseMethodologySections](frontend/src/MethodologyDrawer.tsx) splits sections on `\n---\n`. Without the separator, References would fold into the previous section and not be separately navigable in the drawer.
- **Backdating contract (Phase 11):** `run_scenario(market_date=...)` is the as-of date. When `effective_as_of < today` (resolved via `app/utils/calendar.py::resolve_effective_market_date` — NYSE trading day on or before the requested date), the pipeline switches to vintage-controlled backdated mode. Three things change at once: (1) the events registry is filtered by `event.end_date <= effective_as_of` via `app/factors/analogs.py::filter_events_as_of` (end-date, **not** start-date — start-date filter would let in-progress events leak post-as-of returns through `fetch_event_returns`); (2) yfinance fetches use `end=effective_as_of + 1 day` (yfinance `end=` is exclusive); (3) the warm cache (`get_factor_returns_with_history`) is BYPASSED — call `fetch_factor_returns_with_history(end=...)` directly — because the warm cache keys only on `lookback_weeks` and always returns current data; (4) the narrative call goes through `_analog_grounded_narrative` (no Google Search) and returns empty citations.
- **`adjust_scenario_shocks` is also vintage-controlled.** When the canonical scenario's `market_date < today`, the adjustment refetches with the same `end=canonical.market_date + 1d` and also bypasses the warm cache. Without this, the new P&L would mix backdated shocks with current betas — a look-ahead leak.
- **Effective vs requested as-of dates.** `result.market_date` is the **effective** date (the NYSE trading day used for filtering and data fetches). `result.requested_as_of_date` (new Phase-11 field) is the raw user request. The UI displays both in the backdated-mode banner. The cache key uses `effective_as_of` so a user picking a Saturday hits the same cache as someone picking the prior Friday.
- **`PROMPT_VERSION` is at v10 in code.** v10 added the four Phase-22 factors (EFA/HYG/GLD/SHY) to the LLM payload and EFA to rule 7's overlap enumeration; the registry grew 17 -> 31 alongside (that part rides `events_version`). The v9 contract below still holds. v9 is the shock-semantics contract: the extraction prompt defines shock units/horizon (cumulative episode total return, NOT weekly), rule 4 states the validator's actual blocking behavior (the reasoning-based escape hatch never existed in code — `tests/test_prompts.py` pins this), factor descriptions are horizon-neutral, the extraction payload gains per-event factor returns + window lengths, analog selection is enforced to 2–5 unique events post-hoc (422), periphery shocks gained a hard ±0.75 band, `ScenarioResult` gained `regression_quality` + `analog_event_returns`, and `.T` ticker returns are USD-converted before betas. Treat `app/llm/prompts.py` as authoritative.
- **LLM parametric knowledge is NOT vintage-controlled** even on the backdated path. Disabling Google Search prevents current-news leakage but the Gemini model still "knows" about events after the as-of date (e.g. it knows COVID happened even if as_of is 2018). The UI banner (`frontend/src/AsOfDatePicker.tsx::BackdatedModeBanner`) makes this honest. For sign-off-grade reproducibility, treat the analog envelope and shock magnitudes as canonical; the narrative is illustrative.
- **`ScenarioResult` schema gained four new fields in Phase 11**: `requested_as_of_date` (defaults None), `narrative_mode` ("grounded" | "analog_only", defaults "grounded"), `selected_event_ids` (defaults `[]`), and they're all default-defaulted so v5-cached payloads deserialize cleanly under v6 lazy re-derivation.
- **Firestore vs GCS roles in Phase 11.** GCS scenario_cache (existing) is the de-dup layer for identical-key runs (7-day TTL). Firestore (new, `saved_scenarios/{id}`, `portfolios/{id}/snapshots/{id}`) is user-facing persistence: tagged, named, permalinked, durable beyond the GCS TTL. Saved records store the FULL `ScenarioResult` + analog event details + reproducibility metadata INLINE. Do not rely on a Firestore record to dereference a GCS cache_key — the GCS entry may have expired.
- **Saved scenarios store everything inline (no FK to live state).** `portfolio_holdings` inline (sample portfolio defs in code can drift; saved analytics shouldn't). `analog_events_snapshot` inline (the events YAML can change). `reproducibility` inline (full block: model_id, prompt_version, factor_universe_version, events_version, beta_lookback_weeks, ridge_alpha, narrative_mode, both as-of dates, selected_event_ids, portfolio holdings + key, market data source, nami engine version). Rendering a saved scenario must NOT consult the live event registry or the GCS cache.
- **Firestore composite index requirement.** Tag-filtered scenario listing needs `tags array_contains + created_at DESC`. Missing-index errors are caught and re-raised as `RuntimeError` with the exact `gcloud firestore indexes composite create` command. See [app/data/firestore_store.py::FirestoreStore.list_scenarios](app/data/firestore_store.py).
- **Firestore 1MB doc limit hard-guarded at 900KB safety margin.** Saves over that raise `ValueError` → HTTP 413. Observed payloads << 100KB; the guard catches future regressions of fat ScenarioResult payloads.
- **Permalink `/saved/{id}` (URL: `?saved=<id>`) is admin-protected, NOT a public share link.** Incognito visitors see a 403. UI copy makes this explicit. Multi-user via Firebase Auth would change this in v2.
- **`MIN_ELIGIBLE_ANALOG_EVENTS = 2`** in `app/llm/scenario.py`. Backdating to a date that leaves fewer eligible analogs raises `ValueError` → 422. The pre-2010 portion of `historical_events.yaml` is sparse so very-old as-of dates surface this gracefully rather than failing on a Gemini call.
- **NYSE holiday list is hand-curated in `app/utils/calendar.py`** for 2020-2030. Pre-2020 / post-2030 dates fall through weekend-only logic (acceptable for v1). To extend, add dates to `NYSE_HOLIDAYS` or swap in `pandas_market_calendars`.
- **Holdings look-ahead guard for portfolio snapshots.** `/api/portfolios/{id}/snapshots` rejects future-dated snapshots with 422. A separate cross-validation (`portfolio_snapshot.as_of_date <= scenario_as_of_date`) should be wired when running a scenario against a saved snapshot — currently this happens implicitly when the UI populates holdings from a snapshot and the user picks an as-of date; an explicit server-side guard against `snapshot.as_of_date > scenario_as_of_date` would be a v1.1 hardening.
- **Mock `_MockGeminiClient.propose_shocks_with_retry` accepts `analog_grounded`, `as_of_date`, `selected_analog_events`, and `per_event_returns`.** Tests that exercise backdated paths use `type(gemini).last_analog_grounded` to assert which narrative path was taken.
- **Mock `_patch_market_layer` captures `end=` kwarg** so backdating tests can assert that yfinance was called with the correct effective-as-of + 1 day. The captured dict is the return value of `_patch_market_layer(monkeypatch)`.
- **Overlay state and overlay shell are separate.** Parent hooks (`useOverlay()`) own body-scroll-lock + Esc-to-close. `OverlayShell` owns backdrop click, `role="dialog" aria-modal="true"`, focus trapping, initial focus, focus return, and the optional close header. Dialog/drawer components should pass their meaningful initial focus ref and render only their body content; do not reinstall local focus-return or focus-trap effects in each overlay.
- **Phone-stack pattern for label+icon+input rows.** When a `<label>` contains an icon, text, AND an input (e.g. `.asof-picker label`), the inner content is inline-flex on desktop. On phone you want the icon+text inline on row 1 and the input full-width on row 2 — a flex direction:column on the outer container is NOT enough. Use a 2-column grid on the label with `grid-column: 1 / -1` on the input. See the `@media (max-width: 640px)` block at the bottom of `frontend/src/styles.css`.
- **`.saved-row-head` and `.saved-row-meta` both stack on ≤640px.** Both use `justify-content: space-between` between a left-aligned cell and a right-aligned counterpart; on narrow phones long names would crush the right cell. The phone media-query rule stacks them; the `.saved-open` tap target is bumped to `min-height: 40px` at the same breakpoint since the button is now visually isolated.
- **Phase 12 design system ("Hokusai Deep").** `frontend/src/styles.css` `:root` holds the full palette (`--bg` Prussian indigo, `--accent` sand, `--accent-2` crest-blue, `--up`/`--down`, `--warn`) plus type-scale / radius / shadow / motion / `--focus` / font tokens. The legacy names (`--panel`, `--panel-2`, `--red`, `--green`, `--cyan`, `--amber`) are kept as **aliases** of the new tokens, so every existing `var(--*)` reference re-skinned for free. The alias trick does NOT touch hard-coded literals — new CSS must use tokens, not raw hex/rgba, or it will read as the old theme. CTA (`.primary-button`) is sand-on-ink, NOT red (red is reserved for loss/error).
- **`--cyan`/`--amber` are aliases now, not literal colors.** `--cyan → --accent-2` (crest blue), `--amber → --warn`. "Active" affordances (segmented/nav/tag) are crest-blue on ink. Don't reintroduce literal `#8bd3ff`/`#f6b84b`.
- **The brand mark is the 波 ghost glyph + seigaiha crest** (`App.tsx` `.brand-block`). Both are decorative: `aria-hidden` + `pointer-events: none`. The crest is a CSS `background-image` data-URI wave, not an `<img>`. Keep `.brand-glyph` anchored fully INSIDE the block (small positive `top`/`right` insets + a size that fits the ~280–312px rail) — `.rail`'s `overflow: auto` and `.brand-block`'s `overflow: hidden` clip any off-edge bleed, so negative offsets visibly cut the kanji.
- **SSE run-progress is the "wave propagating" signature — per-item, NOT a continuous track.** Each `.run-progress li.active .dot` emits an expanding `::after` ripple (`@keyframes nami-ripple`); done stages settle to a filled `--up` node. The `<ol>` wraps to rows (and goes vertical ≤640px), so do NOT add a single L→R track spanning it — it would cut across wrapped rows.
- **Results "ripple" reveal fires once per mount, not per re-render.** `.results-stack > *` runs `nami-rise` with a per-child stagger. React keeps these nodes across attribution-toggle re-renders, so the animation only plays when a run first lands. Don't key the stack on `attributionMethod` or it will re-animate on every toggle.
- **`prefers-reduced-motion` is handled by one global block** at the bottom of `styles.css` (neutralizes every animation/transition + `scroll-behavior`). `MethodologyDrawer.tsx` ALSO gates its `scrollIntoView` via `scrollBehavior()` (JS can't read the CSS block). New JS-driven smooth scrolls must call `scrollBehavior()` too.
- **Keyboard focus = global `:where(...):focus-visible` ring** (`--focus` double-ring). Inputs keep `outline: none` + a `:focus` border-color; the ring is the keyboard affordance layered on top. Don't re-add bare `outline: none` to buttons without a `:focus-visible` replacement.
- **`useFocusTrap(ref, active)` ([frontend/src/useFocusTrap.ts](frontend/src/useFocusTrap.ts)) adds ONLY Tab-cycling containment.** It composes with `useOverlay` (scroll-lock + Esc) through `OverlayShell`; do not duplicate it inside individual drawers/dialogs. It reads `ref.current` inside the handler (capture-phase `document` listener) so a late-mounted panel still traps.
- **`useOverlay` reads `onClose` from a ref.** `close` and the Esc effect depend only on `[isOpen]`; an unmemoized `options` object no longer churns the keydown listener every render. The "onClose fires BEFORE `isOpen` flips false" contract is preserved (and tested).
- **Attribution control is a real radiogroup.** `role="radiogroup"`/`radio` + roving `tabIndex` + Arrow keys; the nav math is the pure `nextEnabledMethod()` in [frontend/src/attributionNav.ts](frontend/src/attributionNav.ts) (skips disabled, wraps) — unit-tested in isolation so it doesn't require rendering `App`.
- **yfinance timeout lives at `app/data/market.py::_fetch_prices`** (`timeout=_YF_DOWNLOAD_TIMEOUT_SECONDS`, 30s). This is the ONLY correct place to bound the fetch: `run_scenario`, `adjust_scenario_shocks`, and `compute_envelope` wrap it in `with ThreadPoolExecutor(...)`, whose `__exit__` calls `shutdown(wait=True)` and re-blocks on a hung worker — so a `future.result(timeout=)` would NOT bound the request. One timeout here covers all three (every fetch routes through `_fetch_prices`).
- **`run_scenario` validates `selected_ids ⊆ events` and raises `ValueError` on a hallucinated analog id** (→ 422). Without it, `compute_envelope` raises a bare `KeyError` that the `/run` endpoint (which only catches `ValueError`) would surface as a 500. The SSE path already catches `Exception`.
- **`decompose_scenario` validates the cached payload** (`sub_narratives` must be a non-empty `list[str]`) before trusting it; a corrupted/old-format entry recomputes instead of crashing downstream.
- **AdjustmentPanel slider range is exactly `[p10, p90]`; the Remove button is the only path to 0.** The valid domain `[p10,p90] ∪ {0}` is disjoint when the envelope is one-signed, which a single continuous slider can't represent. The number input snaps into the valid domain on blur; the slider is disabled when the value sits outside `[p10,p90]` (e.g. a removed factor). Rows with envelope `count < 3` additionally disable BOTH inputs and caption "n=K analog observation(s) — keep or remove" (the Remove button stays live) — mirroring the server-side keep-or-remove rule.
- **NOT a bug — do not "fix": weekend/holiday live runs are not mis-flagged as backdated.** `resolve_effective_market_date` returns `requested` unchanged when `requested >= today`, so `is_backdated = effective_as_of < today` is equivalent to `requested < today`. A Saturday live run keeps the live Google-Search path.
- **Phase 12 needed NO `PROMPT_VERSION` bump.** The visual redesign, a11y wiring, and correctness hardening change neither `ScenarioResult`'s shape nor prompt semantics, so cached entries stay valid.
- **Mark-to-market (Phase 13) is a LINEAR overlay, computed CLIENT-SIDE.** The engine is linear in weights, so every dollar figure is `return_field × portfolio_nav` (`PortfolioPnL` has NO dollar fields by design — the frontend multiplies). Per-position `$ P&L = by_ticker_total[t] × NAV` (the weight cancels — no unweighted-return field needed). Don't add dollar P&L to `PortfolioPnL`/`portfolio_pnl`.
- **Marking lives in `app/data/marking.py`.** `currency_for_ticker` (Yahoo-suffix map; `.L` → `GBp`, price ÷100), `fetch_marks` (dedicated **daily RAW close**, `auto_adjust=False`, bounded window, last-valid-close ≤ as_of per instrument + staleness guard), `fetch_fx_to_usd` (explicit pair+inversion map — e.g. JPY via `USDJPY=X` **inverted**), `mark_positions`, `mark_book`. All data-unavailable failures raise `MarkingError` (a `RuntimeError` subclass) → API maps to **503**.
- **MTM marks MUST use raw close, not the adjusted weekly `ticker_prices`.** Valuing a share count needs the actual traded price (`auto_adjust=False`); `market_cache_key` carries an `auto_adjust` flag so raw and adjusted never collide. The flag is appended only for raw fetches, so existing adjusted-close cache keys are unchanged.
- **`run_scenario` quantity mode marks BEFORE the Gemini chain** (`_apply_mtm` / `mark_book` in `app/llm/scenario.py`): the price-derived weights flow into the prompt AND the engine. The GCS scenario cache stores the **return-space canonical + the quantity INPUTS only**; NAV / marks / FX are attached **post-retrieval** (on hit and miss) and are **never cached** — so the same scenario at a different NAV can never serve stale dollars (finding #1 from the design review). The cache key folds `position_quantities` only when present, so weight-only keys are byte-identical to pre-MTM. The NAV-scalar `portfolio_nav` is never keyed and never cached (pure overlay).
- **MTM is fail-closed.** If a requested valuation can't be marked (missing/stale price, unavailable FX) the run raises → HTTP **503**, never a percentage-only result. Best-effort degrade is only acceptable for non-requested display adornments (n/a here).
- **`adjust_scenario_shocks` re-marks** from the canonical's cached `position_quantities` at `canonical.market_date` (vintage-correct) and re-attaches NAV; dollars stay client-side. MTM inputs (quantities/NAV) are **admin-only** (`_resolve_mtm` in `app/api/main.py`). Reporting currency is **USD only** in v1.
- **Portfolio snapshots are weight-only in v1.** Shares/MTM mode disables snapshotting (`PortfolioHistoryPanel` `snapshotDisabledReason`); recording share units in snapshots is a v1.1 follow-up.
- **Phase 13 (MTM) needed NO `PROMPT_VERSION` bump** — marking is post-LLM and all new `ScenarioResult` fields are optional with defaults, so old cached payloads deserialize as return-only.
- **"MTM" vs "notional dollar view" — distinct, don't conflate.** *Mark-to-market* = the admin **Shares (MTM)** path that marks share quantities to the as-of close + FX (server-authoritative `result.portfolio_nav`, shown read-only "marked"). *Notional dollar view* = a **client-side NAV knob in `ResultsPanel`** available to **everyone incl. visitors**: dollars are `field × NAV` recomputed **instantly on change — no re-run, no backend call, never cached/keyed**. `charts.ts::parseNav` accepts `$`/commas/`k`/`m`/`b` and rejects non-positive (no silent `NaN`→null). The pre-run NAV field is gone; NAV lives in the results toolbar.
- **Stressed values are client-side and exact** (`charts.ts::buildPositionValuations`): `stressed_value = value + NAV·by_ticker_total[t]`, `stressed_NAV = NAV·(1+total_pnl)`, `Δ% = delta/value` (= the name's scenario return). The Position-valuation table + NAV→stressed metric render **whenever a NAV exists**, independent of the `%/$` toggle (which only flips the waterfall + headline units). Don't duplicate `$ P&L` into the name-level attribution table (it stays pure `%`).
- **`run_scenario(pinned_event_ids=…)`** (theme-sensitivity subsets): skips `select_analogs`, validates `pinned ⊆ events`, and forces the analog-only (no-Search) narrative via `use_analog_only = is_backdated or (pinned_event_ids is not None)`. **`narrative_mode` keys off `use_analog_only`, NOT `is_backdated`.** Folded into the cache key only when present (non-pinned keys byte-identical).
- **Theme sensitivity is fixed-context narrative Shapley, NOT "more realistic Shapley".** `compute_narrative_shapley` pins the source scenario's analogs across all subsets + skips re-grounding → deterministic `v(S)`. It measures the marginal shock each theme adds *within the original analog context* (illustrative, not causal). Served over SSE **`/api/scenarios/decompose-stream`** (mirrors `run-stream`; emits `{stage:"subset", done, total}` so the UI shows "X / Y subset runs"). Dollarized in the UI as `shapley_value × NAV` when a NAV exists.
- **Phase 14 needed NO `PROMPT_VERSION` bump** — the notional NAV is post-pipeline client display; `pinned_event_ids` is inside-pipeline with an optional input; `ScenarioResult` shape is unchanged.
- **Prompt/schema changes are currently v10.** If a later change affects prompt semantics or `ScenarioResult` shape, bump `PROMPT_VERSION` again and update README, `docs/llm-system-design.md`, `docs/methodology.md`, `CLAUDE.md`, and `AGENTS.md` together. Changing factor `description` strings in `app/factors/universe.py` ALSO requires a PROMPT_VERSION bump (descriptions feed the LLM payload but `factor_universe_version` deliberately ignores them). Engine-math changes do NOT bump it — they ride `regression_spec`.
- **Sample portfolios load cap-weights from a frozen snapshot, NOT hard-coded dicts.** `app/data/sample_portfolios.py` reads `sample_portfolio_weights.json` at import (eager `SAMPLE_PORTFOLIOS = _build_portfolios()`); a missing/incomplete snapshot raises at import (hard-fail by design — never silently equal-weight). Regenerate with `uv run python scripts/refresh_sample_weights.py`. The script caps-weights via yfinance `.info["marketCap"]` per portfolio (single-currency assertion: `msci_world` is all-USD via ex-US ADRs, `japan_equity` all-JPY) and bakes sector/country tags in the same pass. Issuer CSVs (iShares/Invesco) are the *preferred* weight source but their endpoints serve an HTML SPA to headless clients; the JSON's `sources` field records provenance so a networked re-run can upgrade it. Re-weighting changes each book's `holdings` → new `scenario_cache_key` (natural miss), so NO `PROMPT_VERSION` bump.
- **`Portfolio` gained a `benchmark: str | None` field; benchmark P&L is a NON-cached post-cache overlay** (mirrors MTM). `_benchmark_overlay` in `app/llm/scenario.py` runs the benchmark ticker as a one-holding portfolio `{bench: 1.0}` through the result's OWN `factor_shocks` (empty periphery) + the same factor history, then attaches `benchmark_ticker` / `benchmark_pnl` / `active_return = total − benchmark_total` via `model_copy`. It is **best-effort** (a display adornment — logs and leaves fields None on failure, unlike fail-closed MTM) and attaches on BOTH cache hit and miss (the hit path fetches its own vintage-correct returns). On the miss path it reuses the factor history already fetched + the benchmark prices fetched in the same hoisted market batch (executor `max_workers` bumped to 3). Benchmarks are never cached, so the canonical blob never carries them.
- **`adjust_scenario_shocks` now rebuilds the portfolio from `canonical.portfolio_holdings` for ALL portfolios** (dropped the `== "custom"` special-case). Rebuilding *sample* books via `get_portfolio(key)` would silently recompute against the *current* snapshot weights, so after a weight refresh an adjustment of an old cached scenario would drift off its canonical. Because benchmarks are overlay-only, `adjust` can't recover a custom book's benchmark from the canonical — `ScenarioAdjustRequest.benchmark` is **resent by the client** (`AdjustmentPanel` passes `result.benchmark_ticker`); sample books fall back to `sample_benchmark(portfolio_key)`.
- **`CASH` is the zero-exposure sentinel** (`CASH_TICKER` in `sample_portfolios.py`). It is stripped before the yfinance/beta fetch and before LLM periphery selection; `_estimate_betas_cash_aware` runs the estimator on a cash-free (re-normalized — betas are weight-independent) book then injects an all-zero `CASH` beta row, so `portfolio_pnl` carries CASH at exactly 0 contribution while its weight dilutes the rest. In MTM mode a **`CASH` quantity is a USD amount, not shares** (`mark_book` excludes it from `fetch_marks`, marks 1.0 → value == quantity). `validate_holdings`/`validate_quantities` accept the sentinel but **reject an all-cash book** (no market exposure to shock). Do NOT add CASH handling to `app/factors/shocks.py` — it lives in `scenario.py`.
- **Sector/country exposure is a client-side breakdown** from the snapshot's baked `ticker_meta`, served by `GET /api/portfolios/ticker-metadata` (`"Unknown"` for misses so custom books get a complete map). `charts.ts::groupByTag` aggregates `portfolio_holdings` (weight) + `portfolio_pnl.by_ticker_total` (P&L) by tag; the `ExposureBreakdown` component renders it with a Sector/Country toggle. No schema/engine change.
- **Phase 15 needed NO `PROMPT_VERSION` bump** — realistic weights only change cache keys; benchmark/active-return is a non-cached overlay; sector tags + cost-display are display-only; the `CASH` sentinel only affects holdings dicts that contain it (distinct key, old entries unaffected). **Cost basis was deferred** (portfolio-accounting surface that doesn't improve the scenario engine).
- **(Phase 16) The paid-Gemini budget is enforced at ONE chokepoint: `GeminiClient._generate_content`.** All 6 `generate_content` call sites route through it; `app/observability/metering.py::MeteredGeminiClient` overrides it to **reserve → call → reconcile** against a Firestore-transactional `usage_daily/{YYYY-MM-DD}` doc. The endpoints build the metered client via `_metered_gemini()` and inject it through `run_scenario`/`adjust_scenario_shocks`/`compute_narrative_shapley`'s existing `gemini=` param — so internal fan-out (retries, 2^N decomposition reruns) and cache-miss-only spend are all counted, while a cache hit (zero calls) costs nothing. Do NOT wrap the public methods instead — they'd miss calls buried inside `propose_shocks_with_retry`. A real cap hit raises `BudgetExceededError` (→ 429, NOT a RuntimeError/ValueError so the existing handlers don't swallow it); a store/infra error fails OPEN (the rate limiter + run cap are backstops). Reserve is the atomic gate against concurrent overspend; failed calls reconcile conservatively (book the estimate) so they can't escape the cap.
- **(Phase 16) Rate limiting (`slowapi`) is decorator-only on the 5 paid endpoints + `/api/auth/unlock`.** Never throttle the `/{path:path}` SPA catch-all or `/assets` (the app must always load). State is per-instance in memory (fine for max-2 instances). The trusted client IP comes from `app/api/middleware.py::client_ip` (reads `X-Forwarded-For` RIGHT-to-LEFT, returns first publicly-routable hop) — the SAME function keys the durable lockout, so a spoofed left-most XFF entry can't reset either counter. Tests disable the limiter via the autouse `_disable_rate_limit` fixture in `tests/conftest.py`; the dedicated rate-limit test re-enables it with a unique source IP.
- **(Phase 16) `/api/health` is liveness (always 200, no I/O); `/api/ready` is the dependency probe** (bounded `ThreadPoolExecutor` + `future.result(timeout=5)` over Firestore/GCS/Gemini, config+ADC only — NO paid generation). Keep them separate: Cloud Run health checks must stay fast/dependency-free.
- **(Phase 16) `request_id`/`ip_hash` live in `contextvars` (`app/observability/context.py`).** The HTTP middleware sets them and existing module loggers pick them up with no signature change. SSE worker threads do NOT inherit contextvars — both stream endpoints capture `ctx = contextvars.copy_context()` and run the worker via `ctx.run(worker)`; the same propagation carries the metered client. Never store raw IPs — only `hash_ip(...)`.
- **(Phase 16) New Firestore collections: `usage_daily`, `auth_throttle`, `audit_log`.** All single-doc get/set/transaction or single-field order-by — NO composite index needed. `purge_all()` walks the `snapshots` subcollections (chunked under the 500-op batch limit) and deliberately does NOT delete `audit_log` (the trail survives a purge; a final `admin.purge` event is written after). The `InMemoryFirestoreStore` mirrors every method (lock-protected) so the budget/lockout/audit tests run without real Firestore — endpoint tests inject it via `monkeypatch.setattr("app.api.main._firestore_store", ...)` (now also in `test_api.py`/`test_api_adjust.py` because the run endpoints touch the store for the run cap).
- **(Phase 16) `/api/status` REUSES `DISCLAIMER_SHORT`** — `est_cost_today_usd` is admin-gated (None for visitors); `/api/usage` is admin-only. The UX additions (Impact-summary readout, chips, ⌘K palette) are additive: the palette is an accelerator whose commands all have visible controls. No `PROMPT_VERSION` bump for Phase 16.
- **(Phase 18) The ridge is STANDARDIZED (unit-variance) with a raw-unit rescale.** `_solve_standardized_ridge` scales centered factor columns to unit variance, solves, then rescales `beta_raw = beta_std / sigma_f` — the rescale is LOAD-BEARING (it keeps `betas @ raw_decimal_shock` dimensionally unchanged; a column-scale-invariance test pins it). `alpha=0.1` now shrinks all directions homogeneously (`λ/(λ+0.1)` with standardized Gram diag ≈ T−1) instead of crushing low-vol factors. Zero-variance columns get exact-0.0 betas.
- **(Phase 18) Per-ticker NaN masks + 40-week floor.** Factor-side NaN rows drop globally; each ticker then regresses on its own valid rows (grouped by identical mask pattern — the all-complete case stays one solve). A ticker with `< MIN_REGRESSION_WEEKS = 40` valid weeks raises `InsufficientHistoryError` (a `RuntimeError` subclass) naming it → API maps to **422** at `/run`, `/adjust-shocks` (MUST precede the generic `RuntimeError → 422 rerun_required` clause there — mislabeling it would offer a rerun CTA that cannot help), and non-stream `/decompose`.
- **(Phase 18) `estimate_betas_for_portfolio` returns `(betas, stats)`.** `stats` is `dict[str, TickerRegressionStats]` (r2 in [0,1], n_obs, idio_vol_weekly — NOT annualized). `_estimate_betas_cash_aware` passes the tuple through with NO stats entry for CASH; `_benchmark_overlay` discards stats. `ScenarioResult.regression_quality` IS cached with the canonical (deterministic from the keyed vintage — unlike the never-cached NAV/benchmark overlays) and is recomputed on adjustments. `estimate_betas` remains a betas-only thin wrapper for tests.
- **(Phase 18) Periphery shocks are hard-banded.** `validate_shock_proposal` rejects non-finite values and `|shock| > MAX_ABS_PERIPHERY_SHOCK = 0.75` (blocking, retry-then-fail like every validator string). The advisory tier lives in `risk_diagnostics`: `periphery_magnitude` (>0.35), `periphery_dominance`, `low_regression_r2` (<0.30), and `position_loss_exceeds_100pct` (per-position modeled return `by_ticker_total[t]/w_t < −1.0`). NEVER clamp `by_ticker_total` — linearity is the engine contract (attribution sums, Shapley efficiency, client-side dollar view); the diagnostic is the honest flag.
- **(Phase 18) Non-USD ticker returns are USD-converted BEFORE beta estimation.** `app/data/fx.py::convert_weekly_returns_to_usd` compounds `(1+r_local)(1+r_fx)−1` using `marking.py`'s suffix/pair maps (new public accessors `major_currency` + `fx_pair_for_currency`); wired after `compute_weekly_returns` in BOTH `run_scenario` and `adjust_scenario_shocks` with the vintage-correct `end=`. All-USD books pass through identity with ZERO FX fetches; an entirely-missing FX series raises `MarkingError` → 503 (fail-closed — silently regressing local-currency returns would reintroduce the JPY-portfolio-vs-USD-benchmark active-return mismatch this fixes). GBp converts with GBPUSD directly (the /100 cancels in pct_change).
- **(Phase 18) Analog cardinality 2–5 is enforced post-hoc in `run_scenario`** (`MIN/MAX_SELECTED_ANALOGS`, unique ids) right after the unknown-ids guard → ValueError → 422; covers the LLM selector AND the pinned-decomposition path. Deliberately NOT a pydantic `min_length` on `AnalogSelectionOutput` — a constrained Vertex `response_schema` is a live-API 400 risk and adds nothing over the post-hoc check.
- **(Phase 19) `X-Error-Code` header + the SSE `code` field are the machine-readable error contract; detail strings are display-only.** Never reintroduce client-side detail-string matching — the adjust-shocks 422 detail is LLM free text (`rejection_reason`). Header codes (emitted only where status alone is ambiguous): `rate_limited`, `budget_exhausted`, `run_cap`, `rerun_required`, `marking_unavailable`. SSE error-event codes: `run_cap`, `budget_exhausted`, `marking_unavailable`, `validation`; an absent code maps to client kind `unknown`, NEVER `network` (an in-band SSE error arrived over a healthy HTTP 200 connection). `app/api/errors.py::http_error` is the coded-raise helper; `frontend/src/api.ts::deriveErrorKind` is the client map; `frontend/src/errorCopy.ts` is the copy layer.
- **(Phase 19) Paid endpoints must keep their FULL pipeline call inside the `except BudgetExceededError` try** — the metered client raises mid-pipeline (e.g. during decomposition's 2^N subset calls), not just at `_metered_gemini()` construction. The decompose endpoint had exactly this hole (mid-compute budget hit → 500); pinned by `tests/test_api_errors.py::test_decompose_budget_exceeded_mid_compute_is_429`.
- **(Phase 19) `RunCapExceededError` MUST subclass `BudgetExceededError`** (`app/observability/metering.py`) — the endpoints' existing `except BudgetExceededError` handlers catch it; the subclass exists only so `_budget_http_error` can emit `run_cap` vs `budget_exhausted`.
- **(Phase 19) Run/decompose streams are guarded by `frontend/src/runLifecycle.ts`.** `begin()` aborts the prior stream and bumps a sequence; `handleRun`/`handleDecompose` gate progress frames, the final result, catch, AND finally on `isCurrent(seq)` — this closes the stale-stream-overwrites-newer-result race AND stale stepper repaints. `cancel()` aborts without bumping seq so the cancelled run's own finally still resets UI state. Separate lifecycles per action — cancelling a run must not abort a decomposition.
- **(Phase 19) `refreshAccess`/`applyAccess` in `App.tsx` are load-bearing** (replaces the old "intentionally retained" note): `useAccessWatch` polls `/api/access` every 5 min + on visibilitychange (failures swallowed — a network blip must not fabricate a downgrade), every `forbidden`-kind ApiError triggers a re-check, and `applyAccess` raises the session-expired banner ONLY on an un-intentional admin→visitor transition (deliberate lock/unlock passes `intentional: true`).
- **(Phase 19) Two `useOverlay` overlays must never be open simultaneously** — both Esc listeners sit on `window` and would close together. The purge flow therefore CLOSES the ops drawer before opening the type-DELETE-to-confirm `ConfirmDialog`; `openOpsDrawer`/`openMethodology`/`openRailDrawer`/⌘K all close the others first. The toast stack renders at `--z-toast` (120), above the 100/101 backdrop/overlay tier.
- **(Phase 19) Purge must remount BOTH admin panels.** Backend purge deletes scenarios + portfolios + snapshots, but `SavedScenariosPanel`/`PortfolioHistoryPanel` fetch on mount/selection only — App bumps `adminDataEpoch` and passes it as each panel's `key` so deleted records can't linger on screen.
- **(Phase 19) `scrollBehavior()` lives in `frontend/src/motion.ts`** — every JS smooth scroll (MethodologyDrawer sections, run→results auto-scroll, focusUnlock) must import it; the CSS reduced-motion block can't reach JS `scrollIntoView` options.
- **(Phase 19) `.results-toolbar` is sticky ≥1080px under `--z-sticky` (10)** — the three `max-width` breakpoints are untouched (there are now TWO `min-width` queries: 1080 sticky-toolbar, 1440 wide grid). New sticky elements slot below `--z-backdrop`. The stale-results treatment is a class toggle (`.results-stack.is-stale`) on the SAME nodes so the one-shot `nami-rise` stagger does not replay; `filter: saturate()` creates a stacking context, which is safe because the sticky toolbar and `th` live inside it.
- **(Phase 19) Wide screens are WIDE, not capped-narrow:** `--workbench-max` is 1800px (fills a 1920px screen completely; only ultrawides center with gutters), and at `@media (min-width: 1440px)` `.results-stack` becomes a `minmax(0,3fr) minmax(0,2fr)` grid where `.waterfall-card` + `.exposure-card` share a row (everything else spans `1 / -1`). The exposure card renders AFTER the waterfall in DOM (order = visual order, stagger unaffected); a `:not(:has(> .exposure-card))` fallback lets the waterfall reclaim the full row when the breakdown is empty. Prose stays readable via `.narrative p { max-width: 75ch }`, NOT by capping the layout.
- **(Phase 19) `TableScroll` (frontend/src/TableScroll.tsx) is the shared scrollable-table wrapper** — `.table-wrap > .table-scroll` with the right-edge fade gated by `useScrollFade` (`has-overflow` class: shown only while hidden content remains to the right, hides at scroll end). Used by TableCard, valuation, decomposition, ExposureBreakdown, AND the OpsDrawer audit log — don't hand-roll the two divs.
- **(Phase 19) CSV exports go through `frontend/src/csv.ts`,** which applies an Excel formula-injection guard to TEXT cells only (`=`, `+`, `-`, `@`, tab, CR get a leading `'`) — number-typed cells stay raw so decimal columns remain machine-parseable. Don't bypass `toCsv` with hand-rolled string joins.
- **(Phase 19) Decompose SSE idle timeout is 120s vs the run stream's 60s** (subset gaps are full pipeline reruns). Both share `api.ts::streamSse`, which owns abort forwarding, idle timing, frame parsing, and the error ladder.
- **(Phase 19) The market cache stores COMPLETE batches only.** A ticker yfinance flakes on (rate limit/outage) comes back as an all-NaN COLUMN that survives the row-wise dropna; `_fetch_prices` now drops all-NaN columns (making the documented "failed tickers are dropped" contract true) and skips the cache write when `set(columns) != set(requested)` — caching an incomplete batch would poison every run on that window for the 24h TTL (the real-world symptom was `InsufficientHistoryError: NVDA (n=0)`). The n=0 error message now names the transient-fetch cause and says retry; `/run` maps remaining bare RuntimeErrors ("yfinance returned no data…") to a coded 503 `unavailable` (clause must stay LAST — MarkingError/InsufficientHistoryError subclasses take their own statuses).
- **(Phase 19) Saved-analytics endpoints wrap store access in `_store_errors()`** (app/api/main.py) — Firestore failures are google-cloud exception types, NOT RuntimeError (missing database, permissions, bad local creds), and previously leaked as bare 500 "Internal Server Error". The context manager maps them to a coded 503 `unavailable` whose detail names the real cause (incl. the store's composite-index gcloud instructions); HTTPExceptions (404/413/422) pass through untouched. `/api/status` always returns 200 and now honestly flips `ready=false` when the store is unreachable (the old suppress never flipped it). Frontend kind `unavailable` passes meaningful detail through (generic copy only for bare status lines).
- **(Phase 19) vitest gotcha: never write `beforeEach(() => mock.mockReset())` with an expression-body arrow.** `mockReset()` returns the (callable) mock; vitest registers a function returned from a hook as a TEARDOWN callback and will CALL the mock at cleanup — for a `mockRejectedValue` mock that manufactures an unhandled rejection that fails the test with the mock's own error. Always use a braced body.
- **(Phase 19) needed NO `PROMPT_VERSION` bump** — error codes are additive headers/fields with detail strings byte-identical; everything else is frontend display/UX. Disclaimer wording unchanged; SPA catch-all + `/assets` remain un-throttled.
- **(Phase 20) `ScenarioResult.analog_replay` is cached-with-canonical, factor-only, and preserved untouched by `adjust_scenario_shocks`.** Computed in `run_scenario` right after betas (`_analog_replay_block` → `app/factors/shocks.py::analog_replay_pnl`); deterministic from already-keyed inputs (betas ← `regression_spec`, event rows ← `events_version`), zero LLM involvement, additive-with-default → NO `PROMPT_VERSION` bump (Phase-13 precedent). Older cached/saved payloads deserialize with `None` — the UI renders "not computed", NEVER zero. NaN event returns (ETF predates the window) contribute exactly 0 and are excluded from `n_factors_covered`; event-return keys missing from the beta columns are ignored (vintage-subset betas in the replay harness).
- **(Phase 20) The engine-replay validation harness lives in `app/factors/engine_replay.py` and must stay LLM-free** (imports only `app.data.*` / `app.factors.*` / `app.config` — adding an `app.llm` import breaks its no-LLM guarantee). Regenerate BOTH artifacts (`scripts/engine_replay_output.json` + `docs/engine-replay-validation.md`, full regeneration — never hand-edit) with `uv run python scripts/run_engine_replay.py` whenever `REGRESSION_ESTIMATOR_ID`/ridge params, `app/factors/universe.py`, `data/historical_events.yaml`, or `sample_portfolio_weights.json` change; the artifact embeds `regression_spec`/`events_version`/factor-universe version/weights as-of so staleness is diff-visible. Vintage policy: factor columns with <40 non-NaN weeks at the vintage are DROPPED (never zero-filled — same rationale as the SHAP background) and disclosed per pair against the full 26-factor universe; pairs with post-vintage IPO holdings SKIP with a named reason rather than renormalizing around the hole. The network test in `tests/test_engine_replay.py` is `RUN_NETWORK_TESTS=1`-gated with semantic assertions only; sign asserts deliberately exclude q4-trade-war-2018 × defensive_mix (defensives were ~flat that quarter).
- **(Phase 21) `TickerRegressionStats` gained `r2_adj` / `p_eff` / `beta_se` — all defaulted None** so 3-field constructors (test mocks) stay valid. `r2_adj` (dof-honest, can be NEGATIVE) and `p_eff` (ridge effective dof `Σ s/(s+α)`) ARE persisted in the cached `TickerRegressionQuality`; `beta_se` (raw-unit ridge-sandwich SEs on `n−p_eff−1` dof, pinned against statsmodels OLS at α→0) is deliberately in-process only — do not add it to the cached schema without a rendering surface.
- **(Phase 21) `RIDGE_ALPHA` stays 0.1 — dated decision record, do NOT raise it without re-running the sweep.** `α ∈ {0.1,2,8,32}` over the engine-replay harness (2026-07-02, 48 pairs): MAE 3.97/3.90/4.26/5.03%, bias −2.13→−2.79% — flat to α=2, monotonic degradation beyond, because this ridge shrinks toward ZERO and understates betas. Under-determination is handled by disclosure (`r2_adj`/`p_eff` + `low_regression_dof` diagnostic), not blanket shrinkage; informative shrinkage targets (market/sector priors) are the future-work lever. Changing `α` self-invalidates caches via `regression_spec` AND lets in-TTL adjustments of pre-change canonicals recompute under the new value (mixed but TTL-bounded — accepted).
- **(Phase 21) `ScenarioResult.pnl_uncertainty` is the ±1σ idio band — dispersion FLOOR, never a confidence interval.** `band = √(Σ(wᵢσᵢ,idio)²)·√h`, `h` = median selected-analog window in weeks (`shocks.py::portfolio_idio_band` + `scenario.py::_pnl_uncertainty_block`). Shock-independent → cached with the canonical and recomputed to the IDENTICAL value on adjustments (horizon comes from `canonical.analog_event_returns`; old canonicals without it get None). Cross-name and cross-week independence assumptions bias it DOWN — keep the "floor, not CI" framing in any copy. Additive default field → NO PROMPT_VERSION bump.
- **(Phase 21) Three additive diagnostic kinds: `band_coverage`, `scenario_vs_replay`, `low_regression_dof`.** `generate_risk_diagnostics` gained an `analog_replay=` kwarg (canonical's block on the adjust path). `band_coverage` counts MATERIAL shocks only (|shock| ≥ 0.01) and warns when >½ are unbanded, else info. `scenario_vs_replay` is threshold-free (bounds = the analogs replayed on the book) with a 50bps dead zone (`SCENARIO_VS_REPLAY_TOLERANCE`). `low_regression_dof` fires below 3 observations per effective parameter and is silent when `p_eff` is absent (old payloads). The frontend `RiskDiagnostics` component renders kinds generically — extending the Literal + types.ts union is the whole UI wiring.
- **(Phase 20) `cloudbuild.yaml` gates deploys on tests.** `backend-tests` (uv image: `uv sync --frozen --extra dev` → ruff → black → pytest with live-evals ignored, CI-dummy env vars) and `frontend-tests` (`node:22-slim`: `npm ci` → typecheck → vitest) start in parallel (`waitFor: ['-']`) and `build` waits on both — a red suite blocks `gcloud run deploy` regardless of GitHub Actions (which stays as the PR surface but CANNOT block this trigger; the two pipelines are independent). Build timeout 1800s, `machineType: E2_HIGHCPU_8`. Don't remove the gates "for deploy speed" — that reopens the ship-while-red hole.
- **(Phase 22) The factor universe is 26 factors** — Phase 22 added `EFA` (market group) and `HYG`/`GLD`/`SHY` (macro group, decimal PRICE returns unlike the index-level TNX/DXY/VIX/OIL). Adding/removing factors changes `factor_universe_version` (cache lever) AND the descriptions feed the prompt payload → PROMPT_VERSION bump required (v10 here). New-factor launch dates (HYG 2007-04, GLD 2004-11, SHY 2002-07, EFA 2001-08) mean pre-launch analog events carry NaN for them — envelope `count` discloses it; never zero-fill. Frontend `FALLBACK_FACTORS` in `frontend/src/factors.ts` must be extended in the same change.
- **(Phase 22) The event registry is 31 events with 12 valid tags** (`technology`, `volatility`, `credit`, `disaster` added). The dot-com and 9/11 events contribute ENVELOPE evidence only — every sample book skips them in the replay harness (holdings didn't exist / <40 weeks of pre-2000 history), which is expected and disclosed per pair. `events_version` (YAML content hash) invalidates caches on any registry edit.
- **(Phase 22) Dimson lag was evaluated and REJECTED by harness evidence — do not add it without new evidence.** The attenuation hypothesis predicts Japan-book understatement; the harness showed +2.2% OVERstatement (US books −2.4% to −4.3%). Adding `EFA` instead cut Japan bias to +0.7% and MAE 5.9%→5.1%. The dated decision record lives in `docs/methodology.md` (Japan currency paragraph).

---

## What NOT to do

- **Never commit `.env` or any `*.json` that looks like a service-account key.** `.gitignore` covers common patterns; the real mitigation is keeping keys outside the repo (e.g., `~/.gcp/nami-sa.json` or `C:/Users/<you>/.gcp/nami-sa.json`).
- **Never paste terminal output, shell prompts, or absolute filesystem paths into committed files** (README, CLAUDE.md, code comments, anywhere). Such pastes leak the user's Windows username, repo location, and project tree layout — and look unprofessional in a public repo. If you need to show example output, strip prompts (`PS C:\...>`, `$`) and replace paths with `<repo root>` / `~/path/`. The repo had a real incident where a 10-line yfinance session pasted into README's Tech Stack section; the `git grep -n 'PS C:\|OneDrive\|<your-username-pattern>' -- README.md CLAUDE.md` check below catches recurrences.
- **Never put proprietary or third-party product/vendor names in commit messages, branch names, PR text, or committed files** (code, comments, docs, prompts). This includes naming any commercial tool nami is compared against or inspired by — terminals, portfolio/risk platforms, data vendors, etc. Describe the capability generically instead ("portfolio-quality increment", "holdings-management parity", "institutional-grade weights"), never the vendor. The same rule applies to LLM prompt strings in `app/llm/prompts.py` — keep proprietary names out of the model-facing text. Reason: this is a public repo and an educational tool; trading on a vendor's marks invites trademark/association problems and reads unprofessionally. (Keep the names out of THIS file too — don't enumerate the vendors you're avoiding.)
- **A force-push does NOT erase a leaked name.** If a proprietary name (or any sensitive string) lands in a commit, amending + force-pushing only hides it from the branch tip: the orphaned commit stays reachable by SHA on GitHub (often indefinitely), the force-push is logged in the branch activity, and Cloud Build recorded the original SHA. So the mitigation is *prevention* — scan before pushing, not cleanup after.
- **Before any `git push`, run the pre-commit content scan** (see Quality gates section). It catches accidental pastes, project-ID leaks, and embedded credentials by grepping the *staged diff*, not just filenames. Extend it with a private (un-committed) wordlist of vendor names to grep the staged diff before committing.
- **Never auto-commit on the user's behalf** unless explicitly asked. Stage explicit files (`git add file1 file2`), never `git add .` until ignores are verified.
- **Don't phase-jump.** README's Implementation Phases are ordered; each phase must be functional + tested before the next. Don't pull Phase 4 LLM work into a Phase 2 PR.
- **Don't preemptively migrate yfinance → Polygon.** The README has a deliberate one-file-change path in `app/data/market.py` when reliability demands it. Don't introduce abstractions for that swap until it's needed.
- **Don't add Shapley logic to `app/factors/shocks.py`.** Shapley lives in `app/factors/attribution.py::conditional_shapley_attribution`; `shocks.py` only orchestrates the call. Narrative-level Shapley lives in `app/llm/narrative_shapley.py`.
- **Keep `CLAUDE.md` and `AGENTS.md` synchronized.** They are the same agent quick-start for different tool ecosystems. Any operational note changed in one should be copied to the other in the same commit.
- **Don't widen the disclaimer surface or soften its language** — it's load-bearing for the regulatory framing ("scenario explorer" not "stress testing").

---

## Phase status

- [x] **Phase 0** — GCP setup (project, billing, APIs, service account, key, bucket)
- [x] **Phase 1** — Foundation (pyproject, config, market, cache, sample_portfolios, API/web shell)
- [x] **Phase 2** — Factor model (universe, regression, shocks)
- [x] **Phase 3** — Historical analog matcher (events YAML, analogs.py, COVID-verified)
- [x] **Phase 4** — LLM integration (Gemini + grounding + structured output + Scenario/Results UI)
- [x] **Phase 5** — UI build-out (editable Portfolio, Scenario form, Results dashboard with factor/periphery reasoning tables, Methodology rendering `docs/methodology.md`)
- [x] **Phase 6** — Live-LLM evaluation tests (3 network-gated semantic tests) + `docs/backtest_results.md` snapshot template + `docs/methodology.md`
- [x] **Phase 7** — Deploy (Cloud Run + Cloud Build 2nd-gen trigger; public `run.app` link with app-level passcode admin mode)
- [x] **Phase 8** — Advanced attribution: factor-level Conditional Shapley via `shap.LinearExplainer` + `shap.maskers.Impute` against demeaned historical factor returns; experimental narrative Shapley / theme sensitivity (2^N subset Shapley); Results-tab toggle + reasoning table; methodology section with axioms + framing
- [x] **Phase 9** — Streamlit → FastAPI + React/Vite/Plotly.js runtime; yfinance GCS cache + de-duped factor fetch + parallel I/O; in-process warm cache; narrative Shapley parallelization; explicit-only + grouped Shapley variants; React 4-mode attribution toggle; PROMPT_VERSION v4 → v5
- [x] **Phase 10** — Iterative shock-adjustment (sliders + LLM patch with scope classifier, server-side provenance via `cache_key`, zero-removal carve-out, `model_copy`-enforced invariants); scenario speedups (yfinance hoisted above the Gemini chain, parallel Shapley fits); SSE progress streaming via `/api/scenarios/run-stream` with a React stepper UI
- [x] **Phase 10b** — Methodology UX refactor: slide-in `MethodologyDrawer` (replaces inline panel) with section-accordion + scroll-spy nav; `AttributionGuide` card under the attribution toggle with method-comparison + deep link; clickable factor names in the Results shock table that deep-link the drawer to `factor-universe`; expanded `docs/methodology.md` with end-to-end worked example covering all four attribution variants and an attribution method decision table
- [x] **Phase 10c** — Mobile-friendly workbench: responsive breakpoints (phone ≤640, tablet ≤1079.98, desktop ≥1080), off-canvas left rail via `RailDrawer` + hamburger button on tablet/phone, shared `useOverlay` + `useMediaQuery` hooks, mutually-exclusive drawers, baseline + media-query touch-target bumps (44px iOS/Material), Plotly mobile layout (320h / -90° ticks / automargin), iOS Safari `100dvh` fix, a11y baseline (aria-modal + focus return) on both drawers
- [x] **Phase 11** — Dated/backdated scenarios with strict vintage control (NYSE calendar resolution, end-date analog filter, warm-cache bypass, analog-only narrative); Firestore-backed saved analytics (tagged/named/permalinked scenarios + portfolio snapshots, full inline reproducibility); `PROMPT_VERSION` v5 → v6
- [x] **Phase 12** — Distinctive visual redesign ("Hokusai Deep" indigo/sand/crest token system, Fraunces display type, wave-propagation SSE + ripple reveal + seigaiha brand mark) + accessibility (global `:focus-visible`, `useFocusTrap` on all overlays, real input labels, radiogroup attribution control, `aria-describedby` errors, `prefers-reduced-motion`) + correctness hardening (yfinance network timeout, hallucinated-analog → 422 guard, SSE idle timeout, decomposition-cache validation, adjustment-slider domain fix, `useOverlay` listener-churn fix); no `PROMPT_VERSION` bump
- [x] **Phase 13** — Mark-to-market: share-quantity valuation marked to the as-of RAW close, FX-converted to USD (`app/data/marking.py`); NAV + price-derived weights feed the unchanged linear engine and the dollar view is a client-side `field × NAV` overlay; return-space-only caching (NAV/marks never cached → no stale dollars), mark-before-LLM in quantity mode, fail-closed (`MarkingError` → 503), explicit FX pairs + per-instrument mark dates; admin-only `Shares (MTM)` editor + NAV field + `%/$` results toggle + Marks table; no `PROMPT_VERSION` bump
- [x] **Phase 14** — Notional dollar view as an instant client-side NAV knob (everyone incl. visitors; reserved "MTM" for admin shares); original→stressed position valuation table (Weight/Value/Stressed/Δ$/Δ% + impact/worst/best sort, always-on when NAV exists); theme sensitivity hardened + reframed as fixed-context narrative Shapley (pinned analogs + no re-grounding → deterministic) over a new SSE `/decompose-stream` with X/Y progress + dollarized contributions; no `PROMPT_VERSION` bump
- [x] **Phase 15** — Portfolio-quality increment: realistic cap-weights from a frozen, dated snapshot (`scripts/refresh_sample_weights.py` → `sample_portfolio_weights.json`, hard-fail load); benchmark + active return as a non-cached overlay (one-holding portfolio through the run's own shocks; attaches on hit/miss/adjust; client-resent custom benchmark; `msci_world`→URTH, `us_tech_growth`→QQQ, `defensive_mix`→SPLV, `japan_equity`→EWJ); sector/country exposure breakdown (baked `ticker_meta` + `/api/portfolios/ticker-metadata` + `charts.ts::groupByTag`); `CASH` zero-exposure sleeve (zero-beta row, USD-amount in MTM, all-cash rejected) + adjust-from-canonical weight-drift fix; cost basis deferred; no `PROMPT_VERSION` bump

- [x] **Phase 16** — Fintech-grade operational hardening (single-tenant; no accounts/billing) + minimalist UX increment. **Backend:** per-IP rate limiting (`slowapi`, trusted-IP keying) on the 5 paid endpoints + unlock; transactional daily **cost** breaker + run cap + token metering via a `MeteredGeminiClient` chokepoint; durable transactional passcode brute-force lockout; structured JSON logging + `X-Request-ID` middleware (propagated into SSE worker threads via `copy_context`); `/api/ready` dependency probe (distinct from `/api/health`); audit trail + `/api/export` + `/api/admin/purge` (walks snapshot subcollections, preserves `audit_log`) + public `/api/status` + admin `/api/usage`; opt-in CORS + optional Sentry. **Frontend:** answer-first **Impact summary** readout (`charts.ts::buildReadout`), prompt-led suggestion **chips**, ⌘K **command palette** (accelerator — every command also has a visible control). No `PROMPT_VERSION` bump (all post-pipeline / display-only); disclaimer wording unchanged.
- [x] **Phase 17** — Practical risk-workbench cleanup: visitor/admin share sample chips plus Custom state; visitor custom text is accepted only on sample portfolios; factor labels render as human label plus ticker via `/api/factors`; main attribution exposes **Scenario shocks** and **Group totals** only; `Naive algebra` and `Full conditional diagnostic` move to advanced diagnostics; full conditional is correlation credit/non-causal and never drives the headline; deterministic `risk_diagnostics` added; `PROMPT_VERSION` v7 → v8.
- [x] **Phase 18** — Financial-methodology hardening. **Engine:** standardized (unit-variance) ridge with raw-unit rescale + homogeneous shrinkage; per-ticker NaN masks + 40-week history floor (`InsufficientHistoryError` → 422); per-name fit stats (`r2`/`n_obs`/`idio_vol_weekly`) surfaced as cached `ScenarioResult.regression_quality`; new required `regression_spec` cache-key component (the engine-math invalidation lever). **Shock semantics (v8 → v9):** explicit units/horizon contract in the extraction prompt (cumulative episode total returns, not weekly) + honest rule-4 wording; per-event factor returns + window lengths in the payload and on the result (`analog_event_returns`); horizon-neutral factor descriptions; 2–5 analog cardinality enforced post-hoc; periphery hard band ±0.75 + advisory 0.35. **FX:** non-USD ticker returns USD-converted before betas (`app/data/fx.py`) so the Japan book's active return vs EWJ is currency-consistent. **Diagnostics:** `low_regression_r2`, `position_loss_exceeds_100pct` (never clamp), `periphery_magnitude`, `periphery_dominance`. **Adjustments:** count<3 keep-or-remove carve-out (server-side, mirrored by disabled UI sliders). Docs drift sweep (worked-example references, Monday-anchored bars, blocking-validator framing, cache-key listing, MTM staleness wording, theme-sensitivity framing).
- [x] **Phase 19** — Enterprise-grade UX hardening. **Error contract:** additive `X-Error-Code` header + SSE `code` field with a typed frontend `ApiError` layer (per-cause copy + CTAs, request-id refs with copy, the three 429 causes distinguished); decompose endpoint's mid-compute budget hole fixed (was a 500). **Resilience UX:** toast notifications, root `ErrorBoundary`, `ConfirmDialog` (type-to-confirm) replacing native `confirm()`, run/decompose cancellation with an abort+sequence guard closing the stale-stream race, silent admin-cookie-expiry detection with an unlock banner. **Ops console:** admin drawer over `/api/usage` + `/api/status` + `/api/audit` + `/api/export` + guarded `/api/admin/purge` (purge remounts both admin panels). **Ergonomics/presentation:** wide screens stay wide (1800px bound; ≥1440px the waterfall + exposure cards share a 3fr/2fr row; 75ch prose cap), sticky results toolbar, run→results auto-scroll, first-run skeleton + stale-dim re-runs, onboarding empty state, right-aligned tabular numerals + generalized sortable headers + overflow-gated scroll fades (`useScrollFade`/`TableScroll`), per-table CSV export (formula-injection-guarded), consolidated formatters (`format.ts`), Plotly themed from CSS tokens, spacing/z-index tokens + thin scrollbars. No `PROMPT_VERSION` bump.
- [x] **Phase 20** — Measure & show (senior-quant review, tranche 1). **Analog replay range:** factor-only per-analog replay of each selected event's realized factor returns through the run's betas (`app/factors/shocks.py::analog_replay_pnl` → `ScenarioResult.analog_replay`, cached-with-canonical, preserved by adjustments), rendered as an `AnalogReplayStrip` under the Impact summary with min/median/max, per-event coverage, and a factor-only/not-a-forecast caption — closes the "unshocked factors sit at zero" hole and shows each analog's full realized severity against the banded headline. **Engine-replay validation harness:** LLM-free `app/factors/engine_replay.py` + `scripts/run_engine_replay.py` replaying every (registry event × sample book) pair with vintage betas vs realized buy-and-hold USD returns; regenerated artifacts (`scripts/engine_replay_output.json`, `docs/engine-replay-validation.md`) with MAE/bias/sign-rate/Pearson + per-pair skip reasons; decomposes full-run error into engine vs shock-severity layers (first snapshot: r≈0.98, MAE≈4% over 48 pairs — the engine tracks; the severity gap lives in the LLM/band layer). **Deploy gates:** cloudbuild.yaml runs backend (ruff/black/pytest) + frontend (typecheck/vitest) test gates in parallel before build/deploy. **Docs:** methodology "Analog replay" section + Calibration-evidence cross-links, case-studies stale-wording fix + gap-decomposition addendum, `.claude/worktrees/` gitignored. No `PROMPT_VERSION` bump (additive default field; no prompt/schema-semantics change).

- [x] **Phase 21** — Honest uncertainty (senior-quant review, tranche 2). **Idio dispersion band:** ±1σ = `√(Σ(wᵢσᵢ,idio)²)·√h` over the median selected-analog horizon, surfaced as `ScenarioResult.pnl_uncertainty` + a muted suffix on the Impact-summary P&L (dispersion floor, never a CI; shock-independent; cached-with-canonical; identical on adjustments). **Regression hygiene:** ridge effective dof `p_eff`, dof-honest adjusted R² (persisted), raw-unit beta SEs via the ridge sandwich (in-process, statsmodels-pinned); `low_regression_dof` warning below 3 obs per effective parameter. **Alpha decision (evidence-based):** the review's "raise alpha" hypothesis was REJECTED by a harness sweep (shrink-toward-zero degrades stress tracking monotonically above α≈2); default stays 0.1 with disclosure instead of shrinkage. **Evidence diagnostics:** `band_coverage` (K of N material shocks unbanded) + `scenario_vs_replay` (scenario milder/harsher than every analog replayed). No `PROMPT_VERSION` bump (additive default fields + post-processing only).

- [x] **Phase 22** — Engine breadth (senior-quant review, tranche 3a). **Universe 22 → 26** (`PROMPT_VERSION` v9 → v10): `HYG` high-yield credit (the registry is banking/credit-heavy and had NO credit lever), `GLD` gold (flight-to-quality), `SHY` short-duration Treasuries (front-end rates), `EFA` developed ex-US equities. **Registry 17 → 31 events** (+ tags `technology`/`volatility`/`credit`/`disaster`): dot-com break, 9/11, Enron/WorldCom, Shanghai 2007, Tohoku 2011, CHF unpeg 2015, Volmageddon 2018, 2019 trade-war re-escalation, vaccine rotation 2020, rates tantrum 2021, Evergrande 2021, higher-for-longer 2023, Fed-pivot rally 2023, DeepSeek AI-capex shock 2025 — including recovery and rotation episodes. **Harness regenerated on the new baseline:** 124 pairs / 85 computed, MAE 3.25% (was 3.97%), bias −1.40% (was −2.13%), sign 93%, r 0.976; adding EFA cut the Japan book's bias +2.2% → +0.7%. **Dimson lag evaluated and rejected by evidence** (Japan overstates, not understates — dated decision record in methodology). Sequencing note: breadth shipped BEFORE the stressed-beta experiment so that experiment runs against the final baseline.

Source of truth for phase scope: the Phase status list above. (README.md deliberately carries the feature-level view only — it has no phases section; don't reintroduce a dangling `README.md#implementation-phases` link here.)
